# C语言


## 必备计算机基础

计算机：含有CPU芯片的电子设备

CPU：Central Process Unit，中央处理器，由运算器和控制器构成，具有运算和控制两大功能 。运算器负责进行各种数据运算处理（比如算术运算、逻辑运算、位运算等），控制器负责执行代码指令。

计算机硬件系统构成：
- CPU：核心部件，负责运算和控制
- 存储器：存储数据，CPU通过存储器存取数据，存储指令和数据
- 输入设备：输入数据到系统内部
- 输出设备：将系统内部处理的结果数据展示给用户

CPU核心参数：
- 主频率，以Hz为单位，频率的倒数是周期，CPU主频率的倒数就是时期，CPU能够在一个时钟周期内执行完一次基本操作，最简单的一条指令（比如空操作指令）也需要占用一个时钟周期，复杂的指令（比如读写内存、加法运算等）需要占用若干个时钟周期
- 核心数量(Core) ：
CPU能够识别的一条指令就是一串某种组合的二进制序列，比如1010111000等。 
高速缓存(Cache)：容量很小(若干 MB)，读写速度比主内存更快，CPU通常直接与高速缓存交互(命中缓存)。

程序就是一个可执行文件（若干条二进制指令），在磁盘（比如硬盘、U盘、光盘等外存设备）上存储。在运行一个程序时，操作系统会先将可执行文件中的指令和数据加载到内存中，然后CPU再从内存读取指令执行、读取数据进行运算处
理。

存储单位：用来衡量存储数据的多少，常用单位：
- bit：位或比特，最小的存储单位，只能表示一个二进制数字(1或0)
- Byte：字节，1 B = 8 b
- KB：千字节，1 KB = 2^10 B = 1024 B
- MB：兆字节，1 MB = 2^10 KB =1024 KB
- GB：吉字节，1 GB = 2^10 MB = 1024 MB
- TB = 太字节，1 TB = 2^10 GB = 1024 GB
- PB = 皮字节，1 PB = 2^10 TB = 1024 TB

常用进制：
- 二进制(Binary) 前缀加0b/0B，0b1011、0b110
- 八进制(Octal) 前缀加0表示，012
- 十进制(Decimal)  
- 十六进制(Heximal) 0x前缀表示，0x2B3

十进制整数转化为 n 进制的统一方法：除 n 取余，逆序写出。
n 进制转十进制的统一方法：每一位数字乘以该位的权值(第 i 位的权值就是 n 的 i 次方最低位称为第 0 位)，然后求和

要熟练掌握十六进制、八进制、二进制之间互转的方法，因为我们在代码中不建议直接使用二进制，容错性太低，不友好，强烈建议使用十六进制。 


## C 语言核心内容

基本数据类型、变量、常量
运算符、表达式
流程控制结构(顺序结构、选择结构、循环结构)
函数
复杂数据类型（数组、指针、结构体、共用体、枚举）
I/O


## 数据类型(Data Type)

程序的本质就是对数据进行处理，而数据时分类型的，不同类型数据表示形式、存储形态、运算方式、支持的运算都不相同。

C 语言支持的数据类型总体分为两大类：基本数据类型和复杂数据类型。

基本数据类型：
- 整数类型：短整型(short)、整形(int)、长整型(long)、超长整形(long long)
- 浮点类型：单精度浮点型(float)、双精度浮点型(double)
- 字符类型：char
- 特殊类型：void

复杂数据类型：
- 数组
- 指针
- 结构体
- 共用体
- 枚举


## 基本数据类型

基本数据类型分四种：
- 整数类型
- 浮点类型
- 字符类型
- 特殊类型

### 整数类型

#### 整数类型分为四种：
- 短整型：(signed)short,通常占 2 Bytes 存储空间(类型宽度为 2 Bytes),\[-32768,+32767\]
- 无符号短整形：unsigned short.宽度和 short 类型一样，只能表示非负整数,\[0,65535\]
- 整形：(signed)int,通常占用 4 Bytes 存储空间,\[-2^31, +2^31-1\],大概就是正负21.47亿
- 无符号整型：unsigned int，宽度和int类型一样，只能表示非负整数，表数范围为\[0,2^32-1\],约42.94亿
- 长整型：(signed)long,通常占 8 Bytes 存储空间,\[-2^63, +2^63-1\]
- 无符号长整型：unsigned long，宽度和long类型一样，只能表示非负整数，表数范围为\[0,2^64-1\]
- 超长整形：(signed)long long,在绝大部分 C 语言编译器中和 long 是一样的
- 无符号超长整形：unsigned long long，在绝大部分 C 语言编译器中和unsigned long 是一样的

**==整数是以补码形式进行存储和运算处理的，整数的存储形态是补码==** 因为这样的话CPU在对整数进行运算时可以不用管符号问题，符号位和数值位进行同样的处理即可，最终结果也是正确的，CPU更容易实现。

>必须牢记基本数据类型关键字、类型宽度、表数范围、存储形态

C 语言标准并没有明确定义基本数据类型的宽度，只是指定了相对大小，即 longlong>= long>= int>= short，具体大小可以由C语言编译器指定。

对于有符号整数类型，最高位用于表示符号位，规则是：0表正，1表负，其他位用于表示数值位。对于无符号整数类型，没有符号位，所有位都用于表示数值位。


#### **原码、反码、补码**

掌握原码、反码、补码的意义和计算方法

非负整数的原码、反码和补码都一样，但负整数不一样，负整数的计算方法是：原码符号位保持不变，原码的其他位按位取反得到反码；符号位不变，反码加一得到补码。

补码的补码就是原码，也就是说对补码再求一次补码就是原码
负整数补码转换为原码的两种方法：
- 符号位保持，减一取反
- 再求补码（即符号位保持不变，取反加一）

实例
```
1.对于 short 类型数据 1234 D 
原码：0000 0100 1101 0010 
反码：0000 0100 1101 0010 
补码：0000 0100 1101 0010

2.对于 short 类型数据 -1234 D
原码：1000 0100 1101 0010
反码：1111 1011 0010 1101
补码：1111 1011 0010 1110

3.对于 short 类型数据 0
原码：0000 0000 0000 0000
反码：0000 0000 0000 0000
补码：0000 0000 0000 0000
```

注意：整数0的存储形态就是所有位都是0，没有什么正零和负零的说法！


### 浮点类型

用于表示小数，只有两个：
- float：单精度浮点型，占 4 Bytes 存储空间(即类型宽度为 4 Bytes)，表数范围(-3.4\*10^38, -3.4\* 10^-38)和(3.4 \* 10^-38, 3.4 \* 10^38)，记数量级
- double：双精度浮点型，占 8 Bytes 存储空间(即类型宽度为 8 Bytes)，表数范围(-1.7\*10^308, -3.4\*10^-308)和 (3.4 \* 10^-308,3.4\* 10^308)

浮点数的存储形态：按照 **IEEE 754**标准定义的格式


### 字符类型

表示字符数据。

(signed)char：字符型,占 1 Bytes 存储空间(类型宽度为 1 Bytes),用于表示一个英文字符，表数范围\[-128,+127\]
unsigned char：无符号字符型,类型宽度和 char 相同，表数范围\[0,255\]

字符类型也是整数类型


| 英文字符 | ASCII 值 |
| --- | --- |
| a | 97 |
| b | 98 |
| A | 65 |
| B | 66 |
| 0 | 48 |
| 1 | 49 |
| 空格 | 32 |
| 换行符 | 10 |



### 特殊类型

void：空类型，表示不确定的数据类型，不能用于定义变量和常量。




## 变量(Variables)

值可以改变的量。

变量必须先定义或声明，再使用。
定义变量就是在内存中给变量分配一块内存空间(大小就是变量的类型宽度)，变量开始存在。

在程序运行时，变量存放在内存中。

定义变量就是创造变量，语法规则：
```c
数据类型名 变量名;            //没有指定初始值，变量的初始值可能不确定
数据类型名 变量名 = 初始值;    //定义变量并同时初始化(即指定变量的初始值)，建议用法！
```

变量名命名规则：**==必须由英文字母、数字和下划线构成，不能以数字开头，不能为关键字。==**
在 C 语言中，所有标识符区分大小写(大小写敏感)

### 两种命名风格：
- 驼峰命名法：由一个或多个有意义的英文单词构成，不同单词之间没有分隔符，除了第一个单词，后面所有单词的首字母都大写，其他字母都小写
- Linux内核命名法(蛇形命名法)：由一个或多个有意义的英文单词构成，不同单词之间使用下划线分隔，如student_age、total_book_cnt

### 改变变量值的两种方法：
        1.使用赋值运算符(赋值运算符就是=,专门用于给变量赋值,即改变变量的值)
        2.调用 scanf 函数从键盘输入数据赋给变量

### 常用占位符：
- %d：表示十进制的 int 类型数据
- %o：表示八进制的 int 类型数据
- %x：表示十六进制的 int 类型数据



- %hd：表示十进制的 short 类型数据
- %ho：表示八进制的 short 类型数据
- %hx：表示十六进制的 short 类型数据



- %ld：表示十进制的 long 类型数据
- %lo：表示八进制的 long 类型数据
- %lx：表示十六进制的 long 类型数据



- %u：表示十进制的 unsigned int 类型数据
- %hu：表示十进制的 unsigned short 类型数据
- %lu：表示十进制的 unsigned long 类型数据



- %f：表示十进制小数的 float 类型数据
- %lf：表示十进制小数的 double 类型数据
- %c：表示 char 类型数据
- %s：表示字符串
- %g：表示浮点数，去除末尾的 0 和小数点

```c
int 类型整数-123456
原码：1000 0000 0000 0001 1110 0010 0100 0000
反码：0111 1111 1111 1110 0001 1101 1011 1111
补码：0111 1111 1111 1110 0001 1101 1100 0000
```

```c
现有一个float 类型数据：-66.125，其存储形态(IEEE 754)为：
  
十进制小数转换为二进制小数的方法：整数部分按照整数方式转换处理，小数部分的转换方法为：乘2取整，正序写出，直到小数部分为 0
```



## 常量(Constant)

值不可改变的量。
常量的值时固定的，一旦初始化就不再改变(只读，不能被赋值)。


字面量也是常量。

### 字面量的写法：
- 十进制整数：123、123L(long类型)、33U(unsigned int类型)
- 八进制整数：0123、04L、023U
- 十六进制整数：0x123、0x342L、0x51U
- 单个字符：'a'、'4'、'\n'、'\r'、'\t'、'\\'
- 十进制小数：-3.14、-3.14F，前者 double类型，后者 float(后缀F/f)类型
- 科学计数法：-4.56E-3、12.1234e5、52.1234e5F

使用 **sizeof** 运算符可以计算数据类型宽度、变量或常量所占内存空间大小，表达式的值的类型宽度。如printf("%lu\n", sizeof(double));注意：在64位 C 语言编译器下， sizeof 的运算结果是无符号 64位整数，即 unsigned

\ 可以用于转义，它和它后面的字符构成一个转义字符，如：'\n'、'\r'、'\t'

\#开头的行为一条预处理命令(或预编译命令)
### 定义常量的两种做法：
- 宏定义，如：#define PI 1.1415
- 使用 const 关键字定义常量(也称为只读变量)

常量命名风格：由一个或多个有意义的英文单词构成，不同单词之间使用下划线分割，所有字母都大写，如：MAX_DEVICE_CNT




## 运算符(Operator)

实现各种运算处理的特殊符号，也称为操作符。
C 语言提供了非常丰富的操作符，利用它们可以轻松实现对数据进行各种处理
操作数(Operand)：参与运算符处理的数据。

所有运算符都是对操作数进行某种运算得到一个结果数据。
**大部分运算符都只是读取其操作数的值进行运算得到一个新的结果数据，并不会改变其操作数的值，只有少数几个运算符(=、++、--)除了得到运算结果外还由特殊效果(改变其操作数的值)**


### 根据所带操作数个数不同，运算符分为三类：
- 单目运算符(一元运算符)：只有一个操作数，如：++、--
- 双目运算符(二元运算符)：带两个操作数，即左操作数和右操作数，如：+、-、>、&&、=
- 三目运算符(三元运算符)：带三个操作数，只有一个条件运算符(操作数1 **?** 操作数2 **:** 操作数3)

### 根据运算符的功能不同，运算符分为：
- 算数运算符：+(加法)、-(减法)、*(乘法)、/(除法)、%(模运算)、++(自增)、--(自减)、+(正号)、-(减号)
- 比较运算符：> 、>=、<、<=、==、!=
- 逻辑运算符：！(逻辑非)、&&(逻辑与)、||(逻辑或)
- 位运算符：~(按位取反)、&(按位与)、|(按位或)、^(按位异或)、>>(按位右移)、<<(按位左移)
- 赋值运算符：= 、 += 、 -= 、/= 、&= 、|= ，左操作数必须是变量，右操作数任意，该运算符会改变左操作数的值
- 条件运算符：? :
- 逗号运算符：，
- 类型转换运算符：(type)
- 括号运算符：()，优先级最高
- sizeos 运算符：计算类型宽度
- ...........

>sizeof 是编译时运算符，其他运算符基本都是运行时运算符。

### 运算符的两个重要属性：
- 优先级：优先级高的运算符先运算，优先级低的后运算
- 结合方向：在优先级相同时觉得运算先后顺序，从左到右或从右向左，大部分运算符都是从左到右

表达式(Expression)：由变量、常量、运算符、函数调用等构成的式子，单个变量后常量或函数调用也是表达式。


### 算术运算符

操作数和运算结果都是一个

模运算符(%)的运算结果的符号是由它的左操作数决定的，与右操作数无关。

自增运算符(++)和自减运算符(--)都有两种使用方式：前置和后置。它们两除了得到运算结果之外都有一个特殊效果，改变操作数的值(自增运算符会让操作数变大一个，自减运算符会让操作数变小一个)

前置自增运算符和后置自增运算符的运算效果相同（都会使它的操作数的值增大一个），区别是它们的运算结果不同：前置自增运算符的运算结果是其操作数加一之后的值，后置自增运算符的运算结果是其操作数的原值。自减运算符同理。


### 赋值运算符

赋值运算符是双面运算符，运算结果就是右操作数的值(新数据)，但赋值运算符有个特殊效果：它会改变其左操作数的值为右操作数的值，所有它的左操作数必须是左值(变量性质的数据)

使用赋值运算符通常需要的是它的特殊效果，用于给变量赋值

```c
(b = a + 3) = 4；// error，中间运算结果是常量性质的，不能作为左值！
(a + b)  = 5        // error，中间运算结果是常量性质的，不能作为左值!
```

 使用复合赋值运算符：
 ```c
 int a = 1, b = 2;   //这里 = 比赛赋值运算符，只是初始化变量的语法

 a = 3;              // = 是普通赋值运算符
//下面两行代码完全等效
 a = a + 1;
 a += 1;             // += 是复合赋值运算符(加法赋值运算符)

 a %= 5;             // %= 是求余赋值运算符

 b = a /= 2;
 ```



 ### 比较运算符

 比较两个操作数的大小关系，都是双目运算符，运算结果为布尔值(逻辑值)，即(ture)或假(false)

布尔类型可以自动转型为整数类型，规则：真转换为1，假转换为0

```c
a = 1;
b = a-- == 1;
```



### 逻辑运算符

操作数必须是逻辑值(真或假)，运算结果也是一个逻辑值。

其它类型数据可以自动转换为布尔类型，转换规则：非零转换为真，零转换为假。

三个：
- 逻辑非运算符( ! )：单目运算符，如果操作数是真，运算结果就是假
- 逻辑与运算符( && )：双目运算符，当左右操作数都为真时，运算结果为真，其它都为假
- 逻辑或运算符( || )：双目运算符，当左右操作数都为假时，运算结果为假，其它都为为真

注意：逻辑与和逻辑或运算符具有“短路”效果，即短路与、短路或。对于逻辑与运算符，会先计算左操作数(无论右操作数优先级高低)，如果左操作数的值为假，右操作数直接忽略不计，不进行计算处理；对于逻辑或运算符，会先计算左操作数(无论右操作数优先级高低)，如果左操作数的值为真，右操作数直接忽略不计。

==逻辑非、逻辑与、逻辑或的优先级都不相同，**非与或**==


### 逗号运算符

双目运算符，运算过程：先计算左操作数的值，然后计算右操作数的值，运算结果就是右操作数的值

```c
int a, b, c;           //这里逗号是分隔符

a =1, b = 2, c = 3;    //逗号运算符

c = --a, b++;
```

==逗号运算符的优先级最低==


### 条件运算符

用法形式：操作数1  ?  操作数2  :   操作数3

运算过程：首先计算操作数1的值，如果该值为真或非零，接着计算操作数2的值，并且将操作数2的值作为最终运算结果，操作数3不会被计算处理；如果操作数1的值为假或零，接着计算操作数3的值，并且将操作数3的值作为最终运算结果，操作数2不会被计算处理。

条件运算符的实际意义：在某些简单场合可以取代 if .... else ，代码更简洁。

>==条件运算符是唯一一个三目运算符，并且其优先级是**倒数第三**==


### 位运算符

对整数的二进制位直接进行运算处理，非常底层。

==只有整数类型支持位运算符，浮点类型不支持！==

分为下面几个：
- 按位取反运算符( ~ )：单目运算符， 将操作数的所有二进制位进行翻转(0变1，1变0)
- 按位与运算符( & )：双目运算符，将左右操作数的对应位都进行与运算，与运算规则：当两个二进制位都为1是结果为1，其它情况结果都为0。和逻辑与运算符类似。
- 按位或运算符( | )：双目运算符，将左右操作数的对应位都进行或运算，与运算规则：当两个二进制位都为0是结果为0，其它情况结果都为1。和逻辑或运算符类似。
- 按位异或运算符( ^ )：双目运算符，将左右操作数的对应位都进行异或运算，与运算规则：当两个二进制位相同时结果为0，不相同时结果都为1（**相同为零，相异为一**）
- 按位左移运算符( << )：双目运算符，将左操作数的二进制位都往左边移动(高位方向)右操作数指定的位数，如果右操作数为 n，那么高位的 n位将直接丢弃，低位的 n位直接填充0。左移一位，扩大两倍。
- 按位右移运算符( >> )：双目运算符，将左操作数的二进制位都往右边移动(低位方向)右操作数指定的位数，如果右操作数为 n，那么低位的 n位将直接丢弃，高位的 n位直接填充符号位。右移一位，缩小两倍。


#### **位运算符特性**

按位与运算符( & )特性：任何二进制位(0或1)和 0 进行与运算，结果必定是 0 ；和 1 进行与运算，结果必定为其本身。利用该特性可以是实现将整数的某些二进制位清除(reset,清零/置零/复位)的功能。

按位或运算符( | )的特性：任何二进制位(0或1）和1进行或运算结果必定为1，和0进行或运算结果必定为其本身。利用
该特性可以实现将整数的某些二进制位设置（set，置一/置位）的功能。

按位异或运算符( ^ )的特性：任何二进制位（0或1）和1进行异或运算结果必定为它的翻转值，和0进行异或运算，运算结果必定为其本身。利用该特性可以实现将整数的某些二进制位进行翻转操作

```c
//将无符号32位整数变量 n 第17位清零，其它位不变
n &= ~(0x1 << 17);

//将无符号16位整数变量 s1 第9位置一,其它位不变
s1 |= 0x1 << 9;

//将无符号16位整数变量 s2 第10位翻转，其它位不变
s2 ^= 1 << 10;

//将一个无符号32位整数的第13~11位设置位100.其它位不变
unsigned int n;
n = n & ~(0x7 << 11) | (0x4 << 11);
```

位运算符的优先级：按位取反运算符( ~ ) > 按位与运算符( & ) > 按位或运算符( | )，**非与或**

### 运算符优先级：
- 在写表达式时，优先级拿不准，千万别乱猜，多写括号累不死！
- 单目运算符优先级高于双目运算符，记住：独眼龙比两只眼的正常人更狠！
- 三目运算符( ?  : )：优先级倒数第三！
- 赋值运算符的优先级倒数第二！
- 逗号运算符的优先级最低！
- 对应数码运算符，算数运算符 > 比较运算符> 位运算符 > 逻辑运算符 ，记住四个字：算比位逻
- 记住三个字：非与或



### 类型转换(Type )

参与运算的双目运算符，左操作数和右操作数必须类型相同才能进行运算处理，如果类型不同，就必须先进行类型转换将它们转换为相同类型。

类型转换有两种方式：
- 自动类型转换：即自动转型，也称为隐式类型转换，系统自动进行，我们无需进行任何处理。
- 强制类型转换：即强转，也称为显式类型转换，语法规则：(Type)Expression。强制类型转换也是一种运算符。

基本数据类型之间支持自动转型。

```c
short s = 3;
int i;
float f;
s = 123;       //这里会先将 int 类型字面量 123 自动转型为 short 类型数据，然后再和 s 进行赋值运算
i = s;         //这里会先将 short 类型变量 s 自动转型为 int 类型数据，然后再和 i 进行赋值运算
.......

f = (float)i; f = (float)(i); //强转 ,只有当数据时可以不写括号
f = (float)(i + 2);//先加结果为int 类型，再转            
f = (float)i + 2;  先转换，再加
```

对于赋值运算符，若干左操作数类型和右操作数类型不一样，会先将右操作数类型转型为与左操作数类型一致

需要注意的特殊情况：
- 宽度大的整数类型转型为宽度小的整数类型时高位若干字节会被截断
- 相同宽度的有符号整数类型和无符号整数类型之间的转换：只是对最高位的看法不同
- 浮点类型转型为整数类型时直接取整

表达式的值的类型如何确定：
char/short  --> int --> long -->float -->double



## 流程控制结构

流程控制结构是为了控制程序代码的执行流程，分三种：
- 顺序结构：从上往下逐行执行，默认执行流程
- 选择结构(分支结构)：根据条件是否满足选择性的执行不同代码
- 循环结构：重复多次执行若干行代码


### 选择结构

通过 if  语句和 switch 语句实现


**if 语句语法规则**

1. 单分支语法
```C
if(表达式)
  {
    // 如果只有一条语句，大括号省略，通常写！
    // 如果满足条件执行的语句有两行及以上的，必须写大括号，将它们构成代码块，否则 if 只认它后面的第一条语句
  }

  执行流程：先计算表达式的值，如果值为真或非零，就表示满足条件，接着执行 if 后面的代码块；如果值为假或零，表示不满足条件，就不会执行 if 后面的代码块，整个 if 语句直接结束，接着执行后续语句。
```

2.双分支语法（二选一）
```c
  if(表达式)
    {
    
    }
  else
  {
    
  }
  // 执行流程：先计算表达式的值，如果值为真或非零，就表示满足条件，接着就会执行 if 后面的代码块或第一条语句；如果表达式的值为假或零，表示不满足条件，就不会执行 if 后面的代码块或第一条语句，而是去执行 else 后面的代码块或第一条语句。
```

3.多分支语法（多选一）
```C
if(表达式1)  
{  
  // 如果只有一条语句，可以省略不写大括号，但通常建议都写！  
}  
else if(表达式2)  
{  
  // 如果只有一条语句，可以省略不写大括号，但通常建议都写！  
}  
else if(表达式3)  
{  
  // 如果只有一条语句，可以省略不写大括号，但通常建议都写！  
}  
// else if 可以写任意个  
// ......  
else  // 这里的 else 子句可有可无，最多只能写一个  
{  
  // 如果只有一条语句，可以省略不写大括号，但通常建议都写！  
}

if(5 = n)    // 编译阶段报错，方便查找错误
if(5 == n)   // 正确写法
```


空语句（  ;  ）：没有任何实际执行效果，也是合法语句，编译后对应 NOP 指令(空操作指令，与需要 CPU 执行(需要占用一个 CPU 时钟周期)，经常用于语句占位、延时等功能。

代码块(Code Block)：用大括号括起来的若干条语句(可以为空或一条语句)，它们是一个整体，要么都执行一次后都不执行。

常用函数：
-   scanf：scan format，格式化输入，即按照第一个参数指定的格式输入数据
-   printf：print format，格式化输出，即按照第一个参数指定的格式输出数据



**switch语句语法规则**

switch 语句的功能非常单一，它只能实现整数的等值比较。它完全可以被 if 语句替代，存在的意义主要是在某些需要整数等值比较的场合让代码更简洁。

```C
switch(整数表达式)       // 表达式的值必须是整数
  {
    case 整数常量表达式1:
    .......
    .......
    default:           // default 可有可无，最多写一个，一定要写在最后
  }

  // 执行流程：先计算 switch 后面表达式的值，然后从上往下依次和每个 case 比较大小，如果 case 相等，就接着执行它后面的所有语句(包括后面的 case 和 default)，直到执行break 语句或者执行完所有语句，switch 语句执行完成
```


break 语句的两个作用：
- 用在 switch 语句内部，结束整个 switch 语句
- 用在循环体中，结束整个循环



### 循环结构

>循环：loop

主要有三种实现方式：
- while 
- do ... while
- for


**while 语法规则**
```C
while(表达式) // 表达式的值决定了是否进行循环，也称它为循环条件表达式
  {
      // 循环体
      // 如果只有一条语句需要循环执行，大括号可以不写，但通常都写
  }
  // 执行流程：首先计算表达式的值，如果值为真或非零，就表示满足循环条件，接着将循环体执行一次，然后再计算表达式的值，如果值为真或非零，就表示满足循环条件，接着将循环体执行一次，重复今下午该过程，直到表达式的值为假或零，就表示不满足循环条件，while 循环结束
```
死循环：永远无法结束的循环

break 语句和 continue 语句可以在循环体中使用，它们的作用：
- break 语句：结束当前整个循环
- continue 语句：结束本次循环，继续下一次循环；只要执行 continue 语句，当前循环体中的所有后续语句直接跳过，执行下一次循环

结束一个循环的两种做法：
- 不满足循环条件（让循环条件的值为假或零）
- 在循环体在执行 break 语句

死循环通常没有实际意义，通常我们需要精确控制循环次数，通过使用循环变量实现，秘诀：循环变量i从零开始，循环条件写 i < n， 就是 n 次循环。


**do...while循环语法规则**
```C
do
  {
    
  }
  while(表达式)；

  // 执行流程：首先执行一次循环体，然后再计算表达式的值，如果值为真或非零，就表示满足循环条件，接着将循环体执行一次，然后再计算表达式的值，如果值为真或非零，就表示满足循环条件，接着将循环体执行一次，重复今下午该过程，直到表达式的值为假或零，就表示不满足循环条件，do...while 循环结束.
```
while 和 do... while 的区别：while 先判断循环条件，再决定是否执行循环体，while 的循环体可能一次都不执行；do...while 先执行循环体，在判断循环条件，do...while 至少执行一次循环体。 


**for 语法规则**
```C
for(表达式1; 表达式2; 表达式3)
  {
    
  }

  // 执行流程：首先计算表达式1的值，再计算表达式2的值，如果表达式2的值为真或非零，就表示满足循环条件执行一次循环体，然后再计算表达式3，接着再计算表达式2的值，如果表达式2的值为真或非零，就表示满足循环条件执行一次循环体，然后再计算表达式3，重复直到表达式2的值为假或非，循环结束。

  表达式1 通常用于对循环变量进行初始化，C99标准开始支持再这个位置定义并初始化循环变量。可以留空不写
  表达式2 就是循环条件表达式，它的值决定是否继续循环。可以留空不写，表示循环条件永远满足。
  表达式3 通常用于改变循环变量的值，让循环能够结束，可以留空不写。

  for 三个表达式都可以留空不写，但中间两个分号必须写。
```


**goto 语句**

goto 语句：无条件跳转
语法规则:
```C
a:                   // 定义一个标签(Label)，用于标记一个代码位置，没有实际执行效果
printf("123455");
printf("efrfsef");

goto a;              // goto label; 直接跳转到 a 标签处执行
```
goto 语句也可以实现循环
goto 不能跨函数跳转



## 函数(Function)

函数就是一个实现了某个功能的代码块，代表一个功能模块。

模块化编程思想(积木式编程)：任何一个软件项目都可以拆分为若干给功能模块，每一个功能模块用一个或多个函数实现，然后在主函数中按照合适的方式将它们调用起来，就完成了整个软件项目。一个函数就是一个积木块，C 语言项目就是由若干函数构成，函数是 C 语言程序的基本构成单位。

面向过程编程思想：将一个实际问题拆解为若干步骤，每一个步骤用一个或多个函数实现，然后在主函数中将这些函数按顺序依次调用起来，就解决了目标问题。

函数分为两种：
- 自定义函数
- 库函数：别人已经实现(定义)，可以直接调用的函数，细分为：标准 C 库函数，拓展 C 库函数，第三方库函数

标准 C 库提供了大约 300 个函数供调用，掌握常用标准 C 库函数，如：printf、scanf、getchar、time ......


### 自定义函数

重点掌握函数定义、函数调用、函数声明。

函数必须先定义或声明，然后才能调用。
>函数就是一坨实现了某个功能的代码，调用函数就是将这坨代码执行一次，所以要想让一个函数起作用，必须要调用它。函数定义(函数实现)就是将这坨代码写出来。

**函数定义语法规则：**
```C
返回值类型 函数名(形参列表)
  {
    // 函数体
  }
```
通常情况下函数执行完成后会有一个结果数据(在函数体中通过 return 已经实现)，即函数返回值，返回值可以是任意数据类型。如果不需要返回值，返回值类型写 void 

函数命名规则和变量一样。
> 命名要见名知意，遵循统一的主流命名风格。

函数参数就是传给函数内部处理的数据，函数就是对数据(参数)进行处理得到一个结果数据(返回值)。参数也可以没有，此时形参列表直接留空或写 void 。

函数参数分为两种：
- 形参：形式参数，定义函数时写的参数，形参就是一个变量，定义形参的语法和定义普通变量是一样的
- 实参：实际参数，调用函数时写的参数

函数体就是实现函数功能的若干代码

return 语句：让函数返回，后面可以跟一个表达式指定函数的返回值，如果函数没有返回值，也可以直接使用return语句(留空)。

C 语言程序启动后，系统会自动调用主函数(main 函数)， 主函数一旦执行结束返回，程序就执行结束了

主函数返回值的意义：操作系统或其他调用我们程序的程序可以通过读取main函数返回值判断我们程序是否成功执行结束。操作系统建议应用程序在成功执行结束时应该返回零，异常结束时应该返回非零整数值，这样的话系统或其他程序就可以通过获取我们程序的返回值判断是否成功执行。


**函数调用语法规则：**
```C
函数名(实参列表) 
```
==注意==：实参必须和形参一一对应，个数和类型必须相同
函数的实参可以为任意表达式，类型必须和对应的形参相同或兼容。

调用函数时的具体执行流程：先给形参变量分配内存空间，将实参的值转递给形参变量（即用实参初始化形参变量），然后进入函数内部执行，执行完所有代码或执行了 return 语句函数返回，回到函数调用处继续往下执行。在函数返回时，所有形参变量以及函数内部定义的变量所占内存空间将会被自动释放，它们将不复存在。

我们通常只需要定义好 main 函数，在程序启动时它会被系统自动调用，在 main 函数中直接或间接调用其它函数

具有返回值的函数可以构成表达式


**函数声明语法规则：**

函数定义可以写在函数调用之后，或者放在其它源文件或库文件中，只需要在函数调用之前声明该函数。
直接 copy 函数首部，结尾加分号。
函数声明也称为函数原型或函数签名。
编译器在编译源代码时是从上往下进行编译处理的，编译器会根据函数声明或函数调用来检查函数调用的语法是否存在错误。



### 递归函数(Recursive Function)

自己调自己的函数。

每次调用函数系统都会分配一块栈区空间，用于存放函数返回地址、形参变量、函数内部定义的非静态局部变量，在函数执行结束返回时系统会自动释放这块栈区空间。如果一直调用函数而不返回，这些函数占用的栈区空间将越来越大，而系统给每个进程分配的总栈区空间很小(默认若干 MB )，很容易导致总栈区空间耗完，此时如果继续调用函数，就会导致栈溢出崩溃(Stack Overflow)。

>递归思想：是一种解决问题的思想，某些实际问题使用非递归方式(通常是找数学规律)很难解决，但使用递归方式就能轻松解决。

要想用递归函数解决好实际问题，需要把握两个关键点：
- 递推关系
- 递归结束条件

使用递归解决汉诺塔问题
```C
void hanoi(int a, char a, char b, char c) // 参数 n 为圆盘个数，参数 a、b、c 为三根柱子的编号，n 个圆盘初始在 a 号柱子上，游戏目标是将它们都移动到 c 号柱子上（借助 b 号柱子） 
{
  // 递归结束条件
  if(n == 1)
  {
    printf("%c --> %c\n", a, c);  // 将 a 柱上的圆盘直接从 a 柱移动到 c 柱上
    return;
  }
  
  // 递推关系
  hanoi(n - 1, a, c, b);         // 第一步：将 a 柱上的 n - 1 个圆盘借助 c 柱移动到 b 柱上
  printf("%c --> %c\n", a, c);   // 第二步：将 a 柱最下面最大的圆盘直接从 a 柱移动到 c 柱上
  hanoi(n - 1, b, a, c);         // 第三步：将 b 柱上的 n - 1 个圆盘借助 a 柱移动到 c 柱上
}
```




## 变量的作用域和生存期

作用域(Scope)：起作用(即能够访问)的代码范围
生存期(Life Time)：存在的期间，即时间范围

定义变量时给它分配内存空间，它开始存在，当它的生存期到达时，系统将释放其所占内存空间，它将不复存在。

变量只能在其作用域范围内被访问，不在其作用域范围访问它会报错(未声明)

**根据作用域不同，变量分为两类：**
- **局部变量(Local Variables)：在函数内部定义的变量（包括型参变量），它们在作用域就是从定义处开始到所有代码块结束。根据生存期不同，局部变量又细分为静态局部变量和非静态局部变量。**
- **全局变量(Global Variables)：在所有函数之外定义的变量，它们的作用域是当前文件甚至整个项目。根据作用域不同，全局变量又细分为静态全局变量和非静态全局变量**

>通常情况下，我们说局部变量指的就是非静态局部变量，全局变量指非静态全局变量。

在不同作用域范围中，局部变量可以同名，它们是不同的变量，不冲突

在定义变量时使用 static 关键字修饰，就是静态局部变量或静态全局变量。

非静态局部变量和静态局部变量的作用域一样(都是代码块局部范围)，但是生存期不一样，非静态局部变量的生存期是从定义时开始到所在代码块执行结束（“短命鬼”），静态局部变量的生存期是程序的整个执行期间（“长生不老”）。  
非静态全局变量和静态全局变量的生存期一样，都是程序的整个执行期间(“长生不老”），但是作用域不一样，静态全局变量的作用域为它所在的文件(文件范围)，而非静态全局变量的作用域为整个项目范围(项目中的所有文件)

每调用函数一次，非静态局部变量(包括形参变量)就“生死轮回”一次。

关于定义变量不进化初始化的问题：
- 非静态局部变量如果不初始化，处值将不确定的(每次运行程序可能都不相同)
- 静态局部变量和全局变量如何不进行初始化， 系统会自动清零初始化    

编程技巧：
- 利用静态局部变量可以统计函数调用次数，判断是否执行该功能。
- 全局变量可以跨函数共享


多文件编程不同函数的实现(定义)写在不同的文件中， main 函数通常写在 main.c 文件中。创建一个项目文件夹，将该项目的所有文件存放在它里面统一管理。编译时项目的所有文件都参与编译。

==注意==： 声明全局变量和定义全局变量不一样，声明全局变量时一定要使用 extern 关键字，否则编译器会认为是定义全新的变量。声明函数是可加可不加。

同名冲突问题：在相同作用域中，不能同名；但在不同作用域中，可以同名；采用“就近原则”确定访问的变量是谁


### **变量有四种，如何选择使用**
- 作用域尽量小（命名冲突更不容易发生）
- 生存期尽量短（内存空间占用时间更短）

记住：
- 能用局部变量，不用全局变量！尽量少用或不用全局变量(非必要不用全局变量)，因为全局变量会增加功能模块之间的耦合度，作用域很大，很容易造成同名冲突。需要用全局变量的情况：多个函数需要共享访问一个变量时就只能用全局变量，优先选择静态全局变量，除非这些函数在不同的文件中。
- 能用非静态局部变量，不用静态局部变量！

>优秀软件设计思想：高内聚低耦合



## 函数的作用域和生存期

函数就是若干行代码构成的，它也需要占用一块内存空间(在代码区中)。
函数的作用域和生存期与全局变量相同，生存期都是整个程序的整形期间。根据作用域不同，分为静态函数(static修饰)和非静态函数。静态函数的作用域是它所在的原文件，非静态函数的作用域是整个项目。在不同文件中，静态函数可以同名，而非静态函数在整个项目中都不能同名(重复定义错误)。能用静态函数就不用非静态函数，一个函数如果只需要在模块内部调用(一个文件内部)，就应该使用静态函数，所以静态函数也称为模块私有函数。如果一个函数确实需要被不同模块调用(跨文件调用)，就只能使用非静态函数(要尽量少)。

**注意**：声明函数时， extern 关键字可以省略，声明全局变量时必须加

常见面试问题： static 关键字有什么用？



## C 语言程序构建(Build)过程详解

从源文件(.c)转化为最终可执行文件的四道工序：
-   预处理（预编译）：Pre-Compile，由预处理器负责，这一阶段会做两件事：删除源文件中的所有注释内容；将源文件中的所有预处理命令（预编译命令）处理掉。gcc 带上 -E 参数可以只进行预处理并输出预处理结果；
- 编译：Compile，由编译器(Compiler)负责，检查语法问题，将 C 语言源代码转换为汇编代码(.s)。gcc 带上 -S 参数可以进行的该步处理就结束；
- 汇编：Assmembly，由汇编器负责，对上一步得到的汇编语言进行汇编处理得到对应是目标文件(.o)，目标文件中都是二进制指令，但是无法进行单独执行。gcc 带上 -c 参数可以进行的该步处理就结束；
- 链接：Link，由链接器负责，将项目中的所有目标文件、标准 C 库文件、第三方库文件(如果用到的话)链接在一起构成最终的可执行文件。链接阶段常见错误就是找不到某个函数的实现代码。
>注意：项目中的每个源文件都要经过预处理、编译、汇编这三步得到对应的目标文件，然后将它们和用到的库文件链接在一起构成最终的可执行文件。

> 通常所说的编译指的是前三步

>gcc: GNU Compiler Collections, GNU 组织旗下的编译器集合。


预处理命令：也称为预编译命令，就是那些以 # 开头的行。它们会在预处理阶段被预处理器进行相应的处理。常用的有：
-   #include：头文件包含预处理命令，找到目标头文件(.h)并将其内容直接全部 copy 过来替换掉该预处理命令
-   #define：宏(Mcaro)定义预处理命令，将后面所有的宏名直接替换为宏值
-   #ifdef\\#elif\\#endif、#ifndef：条件编译预处理命令,根据条件判断那些代码需要保留下来进行后续编译处理,主要用于项目移植、开关宏、==防止头文件重复包含==等
-   ......

头文件(.h)的作用：写各种声明性的语句，如函数声明、全局变量声明、宏定义、类型声明。
对于系统头文件(C语言、操作系统自带的头文件)时使用尖括号，对于项目中的头文件使用双引号。系统头文件通常存放在/usr/include下。

宏分为两种：
- 普通宏，如：#define PI 3.14159
- 带参数宏，如：#define SUM(a, b) ((a) + (b)), #define MAX(x, y)  ((x) > (y) ? (x) : (y))

**必须掌握头文件的规范写法！！！**

```C
#ifndef _STDIO_H_    //放在头文件重复包含
#define _STDIO_H_


#endif /* stdio.h */
```



## 数组(Array)

数组也是一种数据类型，表示若干个相同类型数据的集合。方便轻松进行批量数据处理。

数组元素(Array Element)：构成数组的数据，通过下标(索引)标识不同元素，最前面那个元素的下标为0，后米娜元素一次加一。
数组长度(Array Length)：数组中元素的数量。

定义数组的语法规则：
```C
   // 定义数组类型的变量，未初始化，如果时非静态局部数组，其元素初值将不确定
  数组类型 数组名[数组长度];   

  // 定义数组类型的变量，称为数组，并初始化，可以只给前面的部分元素指定初值（局部初始化），此时后面的未指定初值的元素将会自动清零初始化
  数据类型 数组名[数组长度] = {第 0 个元素的初值, 第 1 个元素的初值, ......};

  // 定义数组类型的常量，称为常量数组，必须初始化，因为后面不能再赋值(只读)
  const 数组类型 数组名[数组长度] = {第0个元素初值,第1个元素初值,...};    

  // 在定义数组时如果指定了元素初值，可以省略不写数组长度，编译器会自动根据初值个数确定数组长度
  数组类型 数组名[] = {第0个元素初值,第1个元素初值,...};
```

所有数据类型的变量和常量都遵循作用域和生存期规则，数组类型、指针类型、结构体类型、枚举类型、共用体类型都不例外。

数组是一个组合，不能整体访问它，只能访问它的某个元素，通过下标运算符([ ])访问它的元素。

数组所占内存空间大小就是它的所有元素所占内存空间大小之和，即数组长度 \* 类型宽度。数组的存储形态(内存布局)：元素按照下标顺序连续相邻存储。

### 数组常用操作

遍历(Traverse)：按照某种顺序将所有元素都访问一次
查找(Find/Search)：在一个数组中查找某个元素
插入(Insert)：在指定位置插入一个新元素，目标位置之后的所有元素都要往后移动一个位置
删除(Delete)：删除指定位置的元素，目标位置之后的所有元素都要往前移动一个位置
排序(Sort)：升序或降序排序所有元素
逆序(Reverse)：前后位置颠倒排列所有元素

```C
int nums[10] = {5, 3, 4, 1, 2, 6};

int p = 3;           // 插入或查找位置（下标）
int data = 5;        // 要插入的新数据
int len = 6;         // 当前有效长度
int n;               // 查找到数据

scanf("%d", &n);



// 遍历查找

int i;
for(i = 0; i < 10; i++)
  {
    if(nums[i] == n) break;
  }
if(i == 10) printf("Not found!\\n");  
else printf("Found! Index：%d\\n", i);


// 插入新元素：在第 3 个元素处插入新元素

for(int i = len; i > p; i--)
  {
    nums[i] = nums[i - 1];
  }
len++;
nums[p] = data;


// 删除元素：删除第 p 个元素

fro(int i = p;i < len - 1;i++)
{
   nums[i] = nums[i + 1];
}
len--; // 有效长度减一


// 逆序

int tmp;
for(int i = 0;i < len / 2; i++)
  {
    tmp = nums[i];
    nums[i] = nums[len - 1 - i];
    nums[len - 1 - i] = tmp;
  }
```


### 字符数组

C 语言追求精简，没有提供专门的字符串(String)类型，可以用字符数组表示字符串。所以字符数组就有两个用途：
- 表示若干字符，和其他类型数组一样
- 表示字符串

字符串(String) ：若干个字符构成的序列，C 语言规定一个字符串**必须以  '\0'**  字符结束( '\0' 字符是字符串结束标志)
>注意三者的区别：0，'0'，'\0'

字符串在实际项目=开发中非常常用(必用)，C 语言为了方便我们使用字符数组表示和处理字符串，专门提供了一些特殊的语法特性：
- 直接使用字符串字面量(用双引号括起来的若干个字符)初始化字符数组
- 字符串处理库函数(如：strcpy、strlen、strcmp等)
- %s 占位符

```
strcpy(arr, "123452");
// 可以调用 strcpy 函数轻松实现字符串赋值操作，该函数内部就是将它的第二个字符串参数的每个字符依次赋值给第一个字符数组的每个元素，最后的'\0'字符也会参与赋值。  string.h 头文件声明与字符串处理相关的函数
```

常用标准 C 库函数：
- printf
- scanf
- getchar：输入一个字符，比 scanf 更方便简单
- system：执行一个命令(程序)，在 stdlib.h 头文件声明
- 字符串处理相关函数：strcpy、strcmp、strlen，在 string.h 中声明

strcpy : string  copy，字符串拷贝，用于给字符串赋值
strlen : string length，计算字符串的长度（不包含 '\0' 字符）
strcmp : string compare，比较两个字符串的大小关系，返回值是 int 类型，为 0 表示相同，非 0 不同



### 二维数组和多维数组

二维数组：一维数组构成的数组，即每个元素都是一个一维数组的数组。

二维数组从逻辑上可以看成是一个具有若干行和若干列的表格，一维表示有几行，二维表示有几列。

使用二维数组可以轻松表示和处理矩阵(Matrix)

二维数组定义和初始化语法规则：
```c
int nums[3][4];    // 一维长度为 3，二维长度为 4，理解方式：一个长度为 3 的数组，它的给个元素是一个长度为 4 的 int 类型数组

int nums2[2][3] = {{1, 2, 3}, {4, 5, 6}};  // 完全初始化，可以部分初始化其余自动清零    
int nums3[2][3] = {1, 2 ,3 ,4}             // 按行安排初值

// 在指定初值的情况下，一维长度可以省略，二维长度必须写！ C 用于编译器会根据右边的初值进行自动推导出一维长度
```

二维数组的元素通过一维下标和二维下标访问


#### 多维数组

三维数组：二维数组构成的数组
```c
double nums[3][4][5];
```

通用规律：无论定义几维数组，在指定了初始值的情况下，只能省略第一维的长度，编译器会根据初始值自动推到出第一维长度


### 二维字符数组

一维字符数组可以用于表示一个字符串，如果需要表示多个字符串(字符串数组)，可以使用二维数组实现，这在实际项目中很重要。
```c
char name1[] = "张三";
char name2[] = "李四";

char all_names[][31] = {"张三", "李四", "王五", "赵四"};
```




### 中文汉字的编码表示

国标编码：GB 2312/GBK/GB18030，使用 2 Bytes 表示一个中文汉字或符号

全球统一编码方案(字符集)：Unicode、UTF-8、UTF-16，使用 3-5 Bytes 表示一个字符，其中中文占 3 Bytes

中文版 Windows 相同默认使用 GBK 编码，Linux 相同默认使用 UTF-8 编码

乱码根本问题：编码和解码方案不一致

不同操作系统上默认的换行符不同，Linux 默认 \r ,Windows 默认 \r\n



### 数组与函数

数组往往比较大，函数直接返回整个数组开销很大(内存拷贝开销)，所以 C 语言不希望使用数组作为函数返回值。
调用函数时要给形参变量分配新的内存空间，并将实参的值传递给形参变量(内存拷贝)，如果形参是数组，时空开销将很大，所以 C 语言不希望我们使用数组作为函数形参。

```c
void f(double data[100])
// void f(double data[]) //编译器会将数组类型形参直接当作指针类型处理，不关注数组长度，所以不写也行
// void f(double* data)  // 虽然写的是数组形参变量，但 C 语言编译器在编译时会直接当作指针类型处理
{
  printf("%lu\n", sizeof(data));  // 8，在 64 位编译器下，指针类型的宽度固定位 8 Bytes
}
```


## 积累常用函数

- printf
- scanf
- getchar
- time
- system
- rand：生成一个伪随机整数，范围介于[0，RAND_MAX] , RAND_MAX 宏在 stdlib.h 中定义，值通常为 int 类型的最大值。需要配合 srand 函数设置不同的随机数种子(Random Seed)，否则每次运行程序得到的随机数序列将相同，通常直接使用系统当前时间戳作为随机数种子，即 srand(time(NULL))。


## typedef 关键字

用于定义(声明)一个数据类型，主要意义：让类型名更有意义，提升代码的可读性；用更简短的类型名代表很长的类型名
==注意==：在实际项目开发在，类型声明通常写在头文件中
```C
typedef unsigned int junge;   // 声明 junge 类型

junge a = 3;
const junge b = 6;
junge* p1 = &a;
junge nums[7];

typedef char sex_t;


// 使用 typedef 关键字声明指针类型
typedef unsigned long* data;
typedef unsigned long price, *money;  

data p1 = NULL;
money p2 = NULL;
price d = 2344;
```




## 指针(Pointer)


指针也是一种数据类型，用于表示内存地址(Memory Address)。内存地址就是一个整数编号，所以指针就是一个无符号整数。在 32 位编译器上，指针的长度就是 32 位的(4 Bytes)。在 64 位编译器上，指针的长度就是 64 位的(8 Bytes)。
**只要是指针类型，不管几重指针，指针类型占内存 64 位(8 Bytes)**

操作系统将整个内存空间以字节(Bytes)为单位划分为一个个的内存单元，并给所以内存单元按顺序依次编号(从 0 开始)，这个整数编号就是所谓内存地址。

CPU 通过内存地址从内存读\写数据。

>32 位系统上内存长度是 32 位的，最大支持 2^32 Bytes 内存空间，即 4 GB。


指针相关的两个运算符：
- &：取地址运算符，此时是单目运算符，运算结果为操作数的内存地址(指针)。
- *：指针运算符(或解引用运算符)，此时是单目运算符，其操作数必须为内存地址(指针类型的数据)，运算结果就是该内存地址处的数据(指针指向的数据)。

指针类型数据的占位符：%p

内存中每个数据的地址统一采用其首字节地址表示。因为每个数据都有确定的数据类型，数据类型决定了其宽度，指定其首字节地址和宽度就能在内存空间唯一确定它

对于一个多字节数据(比如 short 类型数据等)，根据存放字节序不同， 有两种存储模式：
- 小端存储模式(Little Endian)：低位字节低地址，高位字节高地址
- 大端存储模式(Big Endian)：高字节低地址，低位字节高地址

大部分硬件平台(X86、ARM等)都是采用小端存储模式。


定义指针变量(定义指针类型的变量)的语法规则：
```c
//  * 是指针类型的标识
基类型* 变量名;                 // 定义在指针变量
基类型* 变量名 = 指针类型的值;    // 定义并初始化

// 基类型就是指针所指向的数据的类型

int i = 3;  
char c = 'a';  
double d = -3.14;  
  
  
int* p1;   // 定义 int* 类型的变量 p1 ，未初始化
p1 = &i;   // p1 指向变量 i  
  
char* p2 = &c;  // p2 指向变量 c  
  
double* p3 = NULL;   // 指针变量 p3 的初值为 NULL  
  
p3 = &d; // 将变量 d 的指针（内存地址）赋值给变量 p3，此时就称 p3 指向了变量 d

printf("%lu\n", sizeof(p1));  // 8  
printf("%lu\n", sizeof(p2));  // 8
```
NULL 是指针类型的常量，可以赋值给任何指针，值就是 0 。


 输入一段任意英文内容，统计其中出现次数最多的字符。(经典笔试题)
 ```C
 #include <stdio.h>

int main()
{
	char text[1001];

	/*
	int i;
	for(i = 0; i < sizeof(text) / sizeof(text[0]) - 1; i++)
	{
		//scanf("%c", &text[i]);
		text[i] = getchar();

		if(text[i] == '\n') break;
	}

	text[i] = '\0';
	*/

	scanf("%1000[^\n]", text);

	//printf("%s\n", text);
	
	int char_cnt[128] = {0};  // 统计每个英文字符的出现次数，初始都是 0
	char max = '\0';
	int i;
	
	for(i = 0; text[i] != '\0'; i++)
	{
		char_cnt[(int)text[i]]++;
	}

	for(i = 0; i < 128; i++)
	{
		if(char_cnt[i] > char_cnt[(int)max])
			max = i;
	}

	printf("出现次数最多的字符为：%c\n", max);

	return 0;
}
 ```


### 指针常量和常量指针

指针常量：指针类型的常量，指针本身是常量，但它指向的数据不是常量。
常量指针：常量的指针，指针本身是变量，但它指向的数据是常量(在它心中)。
常量指针常量：常量指针类型的常量，指针本身是常量，它指向的数据也是常量(在它心中)。

>区分指针常量和常量指针的秘诀：从左往右读，碰到 const 读常量，碰到  * 读指针。

```C
int a = 3, b = 4;


// 指针变量
int* p1 = &a;
p1 = &b;
p1 = NULL;


// 指针常量(指针本身是常量，但指针指向的数据本身常量)
int* const p2 = &a;
p2 = &b;    // error,常量不能被赋值
p2 = NULL   // error

*p2 = 5;    // ok, 对 a 赋值 为 5


// 常量指针(指针本身是变量，但它指向的数据是常量)
const int* p3 = &a;   // int const * p3 = &a;  两种写法完全等效

p3 = &b;  // ok， 变量可以赋值
*p3 = 5;  // error，p3 指向的数据是常量(只读的，在 p3 心中)，不能赋值(通过 p3 解引用不行)
a = *p3;  // ok, p3 指向的数据是只读的

b = 5;    // ok


// 常量指针常量
const int* const p4 = &a;   // int const * const p4 = &a;  两种写法完全等效

p4 = &b;   // error
*p4 = 5;   // error
```


指针相关的一些有意义的运算

指针和整数进行加减运算：相当于将指针进行偏移若按个单位(以为基类型宽度为单位)。
```C
int a = 3;
int* p1 = &a, *p2, p3;

p2 = p1 + 1;
p3 = p1 - 3;

```

两个同类型指针进行减法运算：结果的意义为它们之间的距离(以基类型宽度为单位)
```C
int n = p1 - p3;   // n = 3
```

指针强制类型转换：转换指针的基类型
```C
int i = 3555905;

short* p1 = (short*)&i;
char* p2 = (char*)&i;
float* p3 = (float*)&i;

printd("%p %p %p\n");    // 这三个指针的值是一样的，但基类型不同
printf("%hd\n", *p1);    // 46961
printf("%c\n", *p2);     // A
printf("%f\n", *p3);     // 0.000000
```



### 数组与指针

数组的存储形态：使用元素按照下标顺序连续相邻存储，下标小地址小，下标的地址大。


指针数组：指针类型的数组，由若干个指针类型数据构成的数组，数组的每个元素都是指针类型的。
```C
int a = 3, b = 4, c = 5;

int* ps1[5];              // 定义一个长度为 5 的 int* 类型数组 ps1， 未初始化
int* ps2[6] = {&a, &b};   // 局部初始化，后 4 个元素初始值为 NULL

ps2[4] = &c;
++*ps2[4];                // ++c;
*ps2[4]++;                // ps2 数组的第 4 个元素值增大了 4，变量 c 的值不受影响
```


数组指针：数组类型的指针，即指针的基类型可以是数组类型，指向整个数组的指针。定义数组指针的语法规则非常独特，需要重点记！
```C
int(*pa1)[5] = NULL;    // 定义一个数组类型的指针变量 pa1，初始值为 NULL，pa1 可指向任意长度为 5 的 int 类型数组

int nums[5];

pa1 = &nums;            // 将 nums 数组的地址赋给指针变量 pa1，即 pa1 指向了数组 nums
(*pa1)[3] = 9;          // nums[3] = 9; 
printf("%lu %lu\n", sizeof(pa1), sizeof(*pa1));  // 8  20
```


数组名等效于数组首元素(第 0 个元素)的指针。
```C
short nums[5];

// nums 和 &nums 的值相同，但基类型不同， nums 表示数字首元素的指针，即 &nums[0] ，它指向的是一个元素，而 &nums 表示的的是整个数组的指针，它指向的是整个数组

// 数组名当指针
*(nums + 2) = 100;  // 完全等效与 nums[2] = 100;
*nums = 5;

short* p = nums + 2;
*(p + 1) = 3;

// 指针当数组
p[2] = 4;          // *(p + 2) = 4;
p[-2] = 4;          // *(p - 2) = 4;
```
下标运算的本质就是指针运算，记住公式：==a[n] 、n[a]完全等效与 *(a + n)==



分析下面代码的执行结果：
```C
int a[5] = {-5, 3, 4, 1, -2};

((int*)(&a + 1))[-2]++;
&a 是指向整个数组的内存地址，&a + 1 表示数字 a 的内存地址加一，即指向数组 a 的下一个数据的内存地址， (int*) 是将指针的基类型转换从 int 类型，[-2] = [a - 2] 表示将内存地址减去两个 int 类型的数据宽度，在小端存储的情况下，[a - 2] 指向 a[3] 的内存地址，所以该式子等效于 a[3]++
```



### 函数与指针


函数的返回值可以是指针类型
```C
double* f(int a, int b)  
{  
  double d;  
  
  d = (double)a / b;  
  
  return &d;     // 返回非静态局部变量的指针有问题，因为在函数返回后非静态局部变量会被释放，后面在通过其指针访问它将会导致不可预知的结果！
}  

long* f2()
{
  static long cnt = 0;

  cnt++;

  return &cnt;
}


  
int main()  
{  
  double* p1 = f(1, 2);  
  
  // 下面代码将会导致不可预知的结果，因为在函数返回后函数内部定义的非静态局部变量已经被释放，再通过其指针去访问一块已经释放了的内存空间将会导致不可预知的结果。  
  printf("%g\n", *p1);  
  *p1 = 3;  
    
  return 0;  
}
```


函数的形参可以是指针类型。使用指针效率很高，尤其对于很大的数据。如在函数内部只需要对形参指针指向的数据进行**只读**操作，最好将该形参指针定义为**常量指针**,这样做的好处是如果在函数实现在不小心对形参指针指向的数据进行了写操作，编译器将直接报错，可以轻松发现并解决问题。
```C
void p(const int* p);
```

当函数形参为数组形式时，编译器会自动处理为指针类型参数。



**函数指针**

函数的内存地址，函数就是若干条指令构成的，存放在内存代码段中，也需要占用一块内存空间，这块内存空间的首字节地址就是函数的入口地址，即函数指针。

通过函数指针可以调用函数，其实还函数名就是函数指针。
> 掌握定义函数指针、通过函数指针调用函数、声明函数指针类型的语法规则

```C
double sum(double a, double b)
{
  return a + b;
}

double sub(double a, double b)
{
  return a - b;
}

double max(double a, double b)
{
  if(a > b) return a;
  return b;
}

void show(double d)
{
  
}

int main()
{
  // 定义函数指针变量,并初始化，可以指向返回值为 double ，由两个 double 类型形参的函数
  double(*pf1)(double, double) = NULL;
  double(*pf2)(double, double) = sum;    // 初值为 sum 函数的指针
  double(*pf3)(double, double) = &sum;   // 和上面写法等效

  pf1 = sub;   // pf1 = &sub;  等效
  pf2 = max;   // pf2 = &max;  等效
  pf3 = show;  // error
  
  double ret;

  ret = max(4, 9);
  ret = ps2(8, 3);     // 调用函数


  // 先声明函数指针类型，再用这个类型去定义函数指针变量、常量、数组等，代码更简洁
  typedef double(*fun_t)(double, double);
  fun_t f5, f6 = dub;
  const fun_t f7 = sum;
  
  fun_t fs[] = {sum, max, sub};   // 函数指针数组
  ret = fs[1](5, 6);    // ret = max(5, 6);
  
  return 0；
}
```



### 字符指针(char*)

字符型指针有两个用途：
- 表示某个字符的内存地址(和其它类型指针一样)
- 表示一个字符串，一个字符串可以使用其首字符的指针代表，因为一个字符串的所以字符都是连续相邻存储的，首字符地址最小，并且尾字符是'\0' ，只要确定了首字符的指针，就能够唯一确定整个字符串。

```C
char s1[] = "ab12";
const char* s2 = "ab12";  // 推荐写法

s2 = "junge!";
s2 = s1;
s2 = s1 + 2;

char* p1 = s1;
p1[2] = '6';

printf("%s\n", p1);  // ab62 

```

用双引号括起来的若干个字符称为字符串字面量，这个字符串存放在内存常量区中，无论多长多短，它的值就是首字符的指针

段错误(Segmentation fault)：内存非法访问



### 空类型指针(void*)

表示一个不确定基类型的指针，不能直接进行解引用运算。

void* 可以代表任意其它类型的指针，其它类型指针和 void* 指针之间可以自动转型（建议 void* 转型为其它类型指针时使用强制转型，因为 C++ 编译器不支持）(一切皆空,空不是一切)。C 语言可以通过 void* 实现泛型编程(GP, General Programming)
>GP :与具体数据类型无关的编程处理方式。



### 二级指针和多级指针

二级指针：也称二重指针，一个指针类型数据的内存地址(指针)
三级指针：也称三重指针，表示一个二级指针类型数据的内存地址(指针)
```C
short s = 0x1234;

short* p1 = &s;     // 一级指针
short** p2 = &p1;   // 二级指针
short*** p2 = &p2;  // 三级指针

**p2 += 5;  // s += 5;
--***p3;    // --s;
```




## 内存管理(Memory Management)

操作系统给每个进程都分配了一块独立的私有地址空间。每个进程的私有地址空间都分为下面几个区域：
- 栈区(Stack)：容量很小(若干兆空间，很容易栈溢出)，系统自动管理分配和释放，系统不会进行初始化，用于存放形参变量、非静态局部变量等
- 堆区(Heap)：容量很大，由我们程序亲自管理分配和释放，很灵活，非常适合在内存中处理大量数据
- 静态存储区(Static Memory)：容量很大，自动相同管理分配和释放，相同自动进行初始化，不灵活，用于存放静态局部变量、全局变量等
- 常量区(Constant)：只读的，系统自动管理分配和释放，用于存放常量
- 代码区(代码段)(Code)：用于存放代码(指令)

使用 const 关键字定义的非静态局部变量只是名义上的常量(不能通过名字进行写操作)，不在常量区中存放，仍然存放在栈区中。使用 const 关键字定义的静态局部变量和全局变量是真正意义上的常量，在常量区中存放。
```C
const int n = 3;  // 名义上的常量 

int* p = &n;
(*p)++         // n = 4;
```


### 堆区内存管理(动态内存管理)相关标准 C 库函数：
- malloc：memory allocate，申请分配一块堆区空间
- calloc：cell allocate，申请分配一块堆区空间
- realloc：re-allocate，重新申请分配一块堆区空间
- free：释放某块堆区空间
>这些函数都在 stdlib.h 头文件中声明

```C
// 在堆区中申请分配空间存放一个整数

// 注意：变量 n1 在栈区中占 8 Bytes，它指向堆区中的 4 Bytes
int* n1 = (int*)molloc(sizeof(int));
if(n1 == NULL)
{
  // 如果申请分配失败(一般是内存不足)
  perror("malloc fail");   // 自动加冒号换行，会在后面附上失败原因
  //return 1;
  exit(1);                 // 结束程序运行
}

*n1 = 5;
++*n1;
// 如果无需再使用某块堆区空间就应该立即释放
free(n1);


// 在堆区中申请空间存放 double 数组，长度 2000000
double* data = NULL;

data = malloc(2000000 * sizeof(double));
if(data == NULL)
{
  perror("malloc fail");
}
else
{
  data[0] = 3.14;
  data[10000] = -345;

  *(data + 5678) = 123;

  free(data);
}


// 堆区存放字符串
char* s1 = malloc(10000);

if(s1 == NULL)
{
  perror("malloc fail");
}
else
{
  s1 = "wafesr6234r";   // 将导致堆区内存泄漏：s1 指向原来指向的那块堆区空间将无法再使用和释放
  strcpy(s1, "qwerty654");
  
  free(s1);
}




// 在函数内部申请堆区空间并进行数据处理，由调用者负责释放

```


积累常用标准 C 库函数：
- perror：打印错误信息
- exit：结束程序，其参数就是返回值


进程结束后它所占的内存空间(包括堆区、栈区、代码区等)都会都会被系统自动释放，但是我们在程序运行期间使用完堆区空间后应该立即释放，如果每个进程都只申请不释放堆区空间，堆区空间必将逐步耗尽，我们程序如果想要再申请新的堆区空间使用将失败，无法正常工作。


### malloc 和 calloc 的区别
- malloc 只有一个参数，表示需要申请分配的堆区空间的字节数；而 calloc 由两个参数，第一个参数为需要申请分配的数据元素的个数，第二个为每个数据元素的宽度。calloc 更适合用于堆区数组。
- malloc 不会进行初始化，而 calloc 会进行清零初始化。
- malloc 效率更高

realloc 用于重新调整一块堆区空间的大小。在扩大时，如果条件允许，它内部会优先选择就地扩大，此时其返回值和第一个参数是一样的；如果后面没有充足的空闲空间，它就会直接申请分配一块更大的新空间，然后将旧空间中的数据都拷贝过去，并将旧空间释放掉，返回新空间首地址，此时其返回值和第一个参数就不一样。
```C
long* nums = malloc(10 * sizeof(long));

if(nums == NULL)
{
  perror("malloc fail");
  exit(1);
}

  nums = realloc(nums, 5 * sizeof(long));   // 变小
  
  free(nums);
```


### 内存操作相关常用标准 C 库函数
- memset：memory set，将一块内存空间中的每个字节都设置为指定值。
- memcpy：memory copy，内存拷贝，适用于两块内存空间不存在重叠部分的情况。
- memmove：memory move，内存拷贝，适用于两块内存空间存在重叠部分的情况。
- memcmp：memory compare，内存比较，如果两块内存中的每个字节都相同，返回值为 0，否则非零。
```C
int nums[100], nums2[100];

nums[9999] = 3; // 内存溢出，导致不可预知的结果

memset(nums, 0, sizeof(nums)); // 给 nums 数组给个元素设置为 0
memset(nums, 1, sizeof(nums)); // no 1

memcmp(nums, nums2, sizeof(nums));


double d1 = -3.1415, d2;

memcpy(&d1, &d2, sizeof(double));
memmove(&d1, &d2, sizeof(double));

char s[] = "hello,junge666!";
memmove(s, s + 6, 10);  // junge666!
memmove(s + 2, s, 5);   // hehellounge666!

```

经典面试题：memcpy 和 memmove有什么区别？如何选择使用？ 



## 结构体(Struct)

若干个不同类型数据(也可以相同类型)构成的集合。
数据成员(Data Member)：构成结构体的数据，也可以称为属性或字段(Field)
结构体的数据成员也称成员变量

在实际项目中用于表示一件商品的信息、一个学生的信息、一本书的信息等，因为它们都是由多个相同或不同类型的实际构成的。

结构体是自定义数据类型，必须先根据自己需求声明(定义)相应的结构体类型，然后才能使用该结构体类型定义变量、常量、数组、指针等。

### 使用 struct 关键字声明(定义)
声明(定义)结构体类型的语法规则：
```C
// 声明 book 结构体，用于表示书籍信息
struct book
{
  // 定义数据成员
  int isbn;  
  char name[31];  
  float price;  
  short type;  
  int cnt;  
  char* author;
};

// 声明学生信息结构体
typedef struct
{
  int sno;
  char name[30];
  float sight;
} student;

// 声明设备信息结构体
typedef struct device
{
  int id;
  char name[31];
  float sight;
} device;

// 声明结构体类型的同时定义结构体变量和指针
struct person
{
  int id;
  char name[31];
} p1, p2, *p3;

// 匿名结构体
struct 
{
  int id;
  char model[51];
  float price;
} c1, c2;
```

使用结构体类型定义变量或常量
```C
struct book b1;
struct book b2 = {1001, "C语言", 30, 3, 999, "王五"}; // 完全初始化，必须严格按照数据成员定义顺序指定
struct book b3 = {1002, "操作系统"};                // 局部初始化，必须严格按照数据成员定义顺序指定
const struct b4 = {1003, "嵌入式", 29.9, 4, 999, "李四"}; // 结构体常量

struct book b5 = {.price = 49.9 , .isbn = 1004, .name = "计组"}; 指定初始化， gcc 支持


student s1 = {2001, "张三", 4.5};  // 不能写 struct 关键字

device d1;
struct device d2;
```

结构体不能被整体访问，只能访问它的数据成员,直接把结构体的数据成员当成单个变量用即可
访问结构体数据成员的两种方式：
- 通过结构体变量名或常量名访问，必须使用成员运算符（ . ）
- 通过结构体指针访问，必须使用指向运算符(->)
```C
s1.sno = 2002;
strcpy(s1.name, "李四");
s1.sight = 4.9;


student* ps1 = &s1;
ps1->sno = 2002;
strcpy(ps1->name, "李四");
ps1->sight = 4.9;

(*ps1).sno = 2002;
strcpy((*ps1).name, "李四");
(*ps1).sight = 4.9;


// 堆区结构体
ps1 = (student*)malloc(sizeof(student));
ps1->sno = 2005;
strcpy(ps1->name, "赵六");
ps1->sight = 4.8;
```



### 结构体的存储形态和大小计算。

结构体的数据成员按照定义顺序依次存储，先定义的数据成员地址小，后定义的地址大，结构体的总大小大于或等于其所有数据成员的大小之和。

内存对齐机制：为了提升内存中数据的读写效率。
结构体大小计算方法：
- 结构体的最前面数据成员的相对地址是 0 ，每个数据成员的相对地址必须为其宽度和内存对齐系数二者中较小值的整数倍；
- 结构体总大小必须为其最大成员宽度和内存对齐系数二者中较小值的整数倍；
- 数组成员处理方式：相对地址必须为宽度位类型宽度和内存对齐系数二者中较小值的整数倍；
- 结构体成员处理方式：相对地址必须为最大成员宽度和内存对齐系数二者中较小值的整数倍；

64 位编译器上默认对齐系数为 8 ，即按 8 Bytes 对齐；32 位为 4。可以通过 #pragma pack 预处理命令进行修改，对齐系数可以设置为1、2、4、8  。
```C
#pragma pack()         // 恢复默认系数

#pragma pack(push, 2)  
#pragma pack(pop)      // 恢复上一次的系数
```



### 结构体与函数

函数的返回值和参数都可以是结构体类型，但强烈建议使用结构体指针做参数和返回值，效率更高。
```C
struct book  
{  
  int isbn;  
  float price;  
};  
  
void set\_stu(struct book\* b, int isbn, float price)  
{  
  b->isbn = isbn;  
  b->price = price;  
}

void show_book(const struct book* b)
{
  printf("%d %g\n", b->isbn, b->price);
}

// 调用者负责释放，否则造成内存泄漏
struct book* create_book(int isbn, float price)
{
  struct book* b = (struct book*)malloc(sizeof(struct book));  
  b->isbn = isbn;  
  b->price = price;  
  
  return b;
}

int main()  
{  
  struct book b1 = {1001, 39.9};  
  struct book b2 = b1;  
  
  b2 = b1;  
  
  struct book* b3 = create_book(1002, 29.5);  
  
  set_book(&b1, 1003, 40.5);  
  show_book(&b1);  
  
  show_book(b3);  
  
  *b3 = b2;  
  
  free(b3);  
  
  return 0;  
}
```



### 结构体与数组

结构体的数据成员可以是数组，数组的元素可以是结构体类型，即结构体数组。
```C
typedef struct student
{
  int sno;
  char name[31];
  float sight;
} student;

student stus[70] = {{1001, "张三", 4.5}, {1002, "李四", 4.3}, {1003, "王五", 4.6}};
strcpy(stus[0].name, "张三丰");
stus[3].sno = 1004;
strcpy(stus[3].name, "赵六");
stus[3].sight = 4.5;

for(int i = 0; i < sizeof(stus) / sizeof(stus[0]); i++)
  {
    // 两种写法完全等效
    printf("%d %s %g\n", stus[i].sno, stus[i].name, stus[i].sight);
    printf("%d %s %g\n", (stus + i)->sno, (stus + i)->name, (stus + i)->sight);
  }
```



### 结构体与指针

结构体指针
结构体的数据成员可以是指针类型。

```C
typedef struct student  
{  
  int sno;  
  char name[31];  
  float sight;  
  char* addr;  
    
} student;  
  
int main()  
{  
  student s1;  
  
  strcpy(s1.addr, "北京昌平");  // 运行时段错误崩溃！ 因为 s1.addr 指向不明确(野指针)，解决方法：可以先让 s1.addr 指向有效的内存空间再操作，如 s1.addr = (char*)malloc(100);
  
  return 0;
}
```



### 结构体嵌套

结构体的数据成员可以是结构体类型。
```C
typedef struct date
{
  int year;
  int month;
  int day;
} date;

typedef struct school
{
  int id;
  char name[51];
  short type;
} school;

typedef struct student
{
  int sno;
  char name[31];
  date birth;
  school* s;
} student;

int main()
{
  school s1 = {1001, "北京一中", 1}
  school s2 = {1002, "北京二中", 1};

  student stu1 = {20001, "张三", {2003, 1, 1}, &s1};
  student stu2 = {20002, "李四", {2003, 2, 13}, &s1};
  student stu3 = {20003, "赵四", {2003, 5, 26}, &s2};
  student stu4;

  stu4.sno = 20004;
  stu4.name = "王五";
  stu4.birth.year = 2003;
  stu4.birth.month = 4;
  stu4.birth.day = 25;
  
  stu4.s = &s1;
  strcpy(stu4.s->name, "北京四中"); 

  
  student* stu5 = (student*)malloc(sizeof(student));
  
  stu5->sno = 20009;
  stu5->birth.year = 2004;
  stu5->s = &2;
  
  free(stu5);
  
  return 0;
}
```



### 位域成员

```C
struct person
{
  // 位域成员，它们共用 32 位内存空间
  // 位域只支持整数类型
  unsigned int id: 10;
  unsigned int age: 7;
  unsigned int sex: 1;
  unsigned int money: 14;

  // 非位域成员
  char name[31];
  float sight;
};

int main()
{
  struct person p1 = {1001, 33, 1, 3000, 4.5, "张三"};
  
  p1.age = 18;
  printf("%u\n", p1.age);
  
  return 0;
}
```













## 共用体(Union)



也称为联合体。和结构体类似，都是由若干个相同或不同类型数据构成的集合，但它的所有数据成员公用一块内存空间。



```C
typedef union demo
{
  	int i;
  	char c;
  	short s;
  	long l;
  	float f;
  	char arr[5];
} demo;

union demo d1;
demo d2;

d1.i = 123;
d1.l = 123;
d1.f = -3.14;
```



共用体的总大小由它里面宽度最大的成员决定的。

访问共用体中最近一次被赋值的数据成员才有意义。



共用体实际应用：

```C
// 拆分 float 类型数据的 4 个字节
union data
{
    float f;
    unsigned char cs[4];
};

union data d1;

d1.f = -66.123;

for(int i = 3; i >= 0; i--)
{
    printf("%02X", d1.cs[i]);
}



union chec_score
{
    float score;
    short level;
    float offer_rate;
    int stu_cnt;
};
```











## 枚举类型(enum)



用于表示只有有限个确定取值的数据，如性别、学历、政治面貌、省份、会员等级等。

```C

// 声明枚举类型 
typedef enum sex_t
{
    // 定义枚举元素(常量)
    male,
    female
} sex_t;


sex_ s1 = male;

s1 = female;
```



枚举类型本质就是 int 类型，只是取值没有 int 类型多，并且给每个值定义了一个有意义的名字(即枚举元素)



```C
enum sex_t{male, female};

enum xueli 
{
  // male    // error，在不同枚举类型中枚举元素不能同名！
  chuzhong,
  gaozhong,
  benke,
  dazhuan,
  yanjiusheng
};

// 枚举元素就相当于一个 int 类型的常量

int main()
{
  enum sex_t s1 = female;

  printf("%lu\n", sizeof(s1));  // 4
  printf("%d\n", s1);
  printf("%d\n", male);    // 0
  printf("%d\n", female);  // 1

  return 0;
}
```











## IO



Input/Output，输入/输出



I/O分为三种：

- 标准I/O：Standard Input/Output,从标准输入设备(键盘、手写板、读卡器...)上读取数据到内存中就是标准输入，将内存中的数据打印到标准设备上(终端)展示就是标准输出。
- 文件I/O：File Input/Output，从磁盘文件中读取数据到内存中就是文件输入，将内存中的数据写入到磁盘中保护就是文件输出。
- 串I/O



### 标准 I/O



分为：

- 标准输入：通常采用行缓冲机制，通过调用 scanf、getchar等标准 C 库函数实现
- 标准输出：通常采用行缓冲机制， 通过调用 printf、putchar 等标案 C 库函数实现
- 标准错误输出：实际效果和标准输出类似，但它无缓冲，通常用于输出异常或错误信息。通过调用perror、fprintf 等标准 C 库函数实现



#### 三种不同缓冲机制：

- 无缓冲：没有缓冲，直接处理
- 行缓冲：每遇到 '\n' 换行符就处理一次，缓冲区满了也会处理一次
- 全缓冲：等缓冲区满就处理一次

fflush(stdout)     刷新标准输出缓冲区



进程正常结束是系统会自动刷新其所有 I/O 缓冲区



scanf 函数的内部机制：它只是从标准输入缓存区中提取它期望类型的数据，如果提取成功，它的返回值将是成功的数据个数；如果提取失败，它的返回值将是 0 。当标准输入缓存区中没有任何数据时，它会等待直到缓冲区中有数据为止。





所谓命令就是一个程序，存放在 PATH 环境变量对应的路径下







### 文件 I/O



文件(File)：就是磁盘上的一份数据。



文件读/写操作三步：

1. 打开文件，调用 fopen 函数实现
2. 读/写文件，调用 fread、fwrite 等函数实现
3. 关闭文件，调用 fclose 函数实现





#### 文件 I/O 常用标准 C 库函数：

- fopen：file open，打开文件，返回值为文件指针(FILE*)，如果打开文件失败，返回值为 NULL
- fclose：file close，关闭文件
- fgetc：file get char，从文件中读取一个字符
- fgets：file get string，从文件中读取一个字符串
- fscanf：file scanf，用法类似 scanf 函数，从文件中按指定格式读取数据
- fread：file read，从文件中读取任意类型的数据
- fputc：file put char，向文件中写入一个字符
- fputs：file put string，向文件中写入一个字符串
- fprintf：file printf，向文件中按指定格式写入一个字符串
- fwrite：file write，向文件中写入任意类型数据,该函数本质就是将内存中的一块数据直接写入文件，其第三个参数的意义为期望写入的数据的个数，返回值表示实际成功写入数据的个数，所有返回值永远小于或等于第三个参数
- fseek：file seek，设置文件读/写位置指针
- rewind：将文件读/写位置指针移动到文件开头(第 0 字节处)
- ftell：file tell，获取当前文件读/写位置指针
- feof：file end of file，判断当前文件读/写位置指针是否已到达文件末尾
- fflush：file flush，刷新文件缓冲区



#### 文件打开模式：

- w：write，写模式，如果目标文件不存在，fopen 函数会自动创建一个新的文件；如果目标文件已存在，fopen 会清空其所有数据
- r：read，读模式，如果目标文件不存在或不可读，fopen 函数将返回失败(返回值为 NULL)
- a：append，追加写模式，如果目标文件不存在，fopen函数会自动创建一个新的文件；如果目标文件已存在，fopen函数不会清空其数据内容。写入位置指针初始在文件末尾，专门用于向文件中追加新数据





写文件操作

```C
char file_path[] = "./data1";

fopen("./data1", "w");
FILE* fp = fopen(file_path, "w");  // 以 w 模式打开 ./data1 文件

if(fp == NULL)
{
    perror("打开文件失败");
    exit(1);
}

// 向文件中写入三个字符
fputc('a', fp);
fputc('b', fp);
fputc('c', fp);

// 向文件中写入以字符串
fputs("def123", fp);

// 格式化写入字符串,非常适合将其他类型数据转换为字符串并写入文件的场合
double d = -3.14;
int i = 123456;
char c = 'm';
fprintf(fp, "%g %d %c\n", d, i, c);

// 写入任意类型的数据
fwrite(&d, sizeof(double), 1,fp); // 写入一个 doble 类型数据



// 关闭文件
fclose(fp);
```





读文件操作

```C
// 从文件中读取两个字符
char c1, c1;
c1 = fgetc(fp);
c2 = fgetc(fg);
printf("%c %c\n", c1, c2); 

// 从文件中读取一个字符串
char s1[100];
fgets(s1, sizeof(s1), fp);   // 最多读取 99 个，遇到 \n 停止，末尾自动加 \0

fseek(fp, 12, SEEK_CUR);     // SEEK_CUR 当前位置，SEEK_SET/END
fscanf(fp, "%lf %d %c", &d, &i, &c);

// 读取任意类型的数据
fread(&d, siaeof(double), 1, fp);  // 读取一个 double 类型数据
```



文本文件：内部都是字符类型数据构成的。

二进制文件：内部含有非字符类型的数据(如int、float、double、结构体等)，使用文本编辑器(如:vi/vim、记事本)打开会显示乱码。







### 串 I/O



即字符串 I/O，string I/O



通过 sscanf 和 sprintf 函数实现。



sscanf 函数可以实现将字符串转化为其它类型数据的功能。sprintf 函数可以实现将其它数据转化为字符串的功能。



```C
char data[] = "-3.14-123456-m";

double d;
int i;
char c;

sscanf(data, "%lf-%d-%c", &d, &i, &c);

d = -5.8;
i = 676;
c = 'a';

char s1[100];
sprintf(s1, "%g/%d/%c", d, i, c)

```









标准输入、标准输出、标准错误输出是三个特殊文件，并且它们已经被系统中的打开，对应的文件指针分别是：stdin、stdout、stderr。



```C
// 效果相同
printf("werthgfd\n");
fprintf(stdout, "wertfd\n");
fputs(stdout, "wesrdd\n");

// 
char c;
c = 9
scanf("%c", &c);
c = fgetc(stdin);
fread(&c, 1, stdin)
    
//
fprintf(stderr, "error?");
fclose(stdin);  // 关闭标准输入
```







