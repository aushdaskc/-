---
[typora]()-copy-images-to: image
typora-root-url: image
---

数据结构与算法是编程指导思想，指导我们写出更高效、更优化、更优雅的代码，与具体编程语言为无关，所有编程语言都能实现，只是语法不一样。



# 算法(Algorithm)



算法：解决问题的方法。如：排序算法、加解密算法、数据压缩算法等。



算法的描述方法：

- 自然语言：汉语、英语等。
- 伪代码
- 编程语言：C、C++、Java、Python等。



用具体编程语言描述算法就是实现算法。



## 算法优劣评估的标准：

- 时间复杂度：基本操作次数与问题规模 n 的关系，采用 O 记法，常见时间复杂度有：O(1)、O(log n)、O(n)、O(n log n)、O(n²)
- 空间复杂度：额外占用的内存空间(不考虑算法需要处理的数据本身所占的内存空间)与问题规模 n 的关系，采用 O 记法，常见空间复杂度有：O(1)、O(log n)、O(n)、O(n log n)、O(n²)



> <u>**基本操作数通常就是循环次数，问题规模 n 就是算法需要处理的数据个数**</u>

O(1)<O(log n)<O(n)<O(n log n)<O(n²)，O(1)也称为常量级，O(log n)对数级，O(n)线性阶，O(n log n)



在计算时间和空间复杂度时，通常会得到一个关于n的多项式，直接忽略低次项和系数即可。



时间复杂度和空间复杂度都是越小越优。



> 时间和空间不可兼得，可以以时间换空间，空间换时间。



```C
// 交换两个变量值

int a = 3, b = 4;


int t;
t = a;
a = b;
b = t;


a = a + b;
b = a - b;
a = a - a;
```





![时间复杂度对比图](..\image\时间复杂度对比图.jpg)









## 排序(Sort)算法

常用排序算法：

- 冒泡排序算法
- 选择排序算法
- 插入排序算法
- 快速排序算法



**必须牢记常用排序算法的时间复杂度、空间复杂度、稳定性。**

排序算法的稳定性：对于值相同的元素，在排序算法处理前后其相对位置不会发生改变就是稳定的排序算法，否则就是不稳定的排序算法。





### 冒泡排序算法实现：

```C
// 函数功能：对任意长度的整型数组进行任意排序（采用冒泡排序算法）
void bubble_sort(int* data, size_t n, int order) // order 为 0 升序，非 0 降序
{
    int i, j, tmp;
    int flag;
    
    for(i = 0; i < n - 1; i++)
    {
        flag = 1;
        for(j = 0; j < n - 1 - i; j++)
        {
            
            if(order ? data[j] < data[j + 1] : data[j] > data[j + 1])
            {
            	tmp = data[j];
                data[j] = data[j + 1];
                data[j + 1] = tmp;
                flag = 0;
            }
        }
        if(flag == 1) break;
    }
}
```

冒泡排序算法平均时间复杂度为 O(n²)，最好情况为O(n);空间复杂度为 O(1)，稳定。





### 选择排序算法



“擂台“思想



```C
void select_sort(int* data, size_t n, int order)
{
    int i, j, tmp, index;
    
    for(i = 0; i < n - 1; i++)
    {
    	index = i;
        for(j = i; j < n; j++)
        {
            if(order ? data[j] > data[index] : data[j] < data[index])
                index = j;
        }
        if(index != i)
        {
            tmp = data[i];
        	data[i] = data[index];
        	data[index] = tmp;
        }
    }
}
```

选择排序算法平均时间复杂度为O(n²)，空间复杂度为O(1)，不稳定。







### 插入排序算法



类似玩扑克牌取牌时的操作，每次都是取一张新牌，然后将其插入到已经有序的牌中，保证继续有序。



插入排序不断重复的操作：将一个新数据插入到一个有序序列中，保证新序列仍然是有序的。



```C
void insert_sort(int* data, size_t n, int order)
{
    int i, j, tmp;
    
    for(i = 1; i < n; i++)
    {
        tmp = data[i];
        for(j = i - 1; j >= 0 && (order ? data[j] < tmp : data[j] > tmp); j--)
        {
                data[j + 1] = data[j];
        }
        data[j + 1] = tmp;
    }
}
```

插入排序算法的平均时间复杂度为O(n²)，空间复杂度为O(1)，稳定。





### 快速排序算法



任意取一个参考数据(通常取最前面或中间的)，将其它所有比它小的数据放到它的左边，比它大的数据放到它右边，这个处理过程称为一趟快速排序处理。然后分别对右边和左边进行同样的处理。



```C
void quick_sort(int* data, size_t n, int order)
{
    int i = 0, j = n - 1;
    int k = data[0];
    
    // 递归结束条件
    if(n < 2) return;
    
    while(j > i)
    {
        while(i < j && (order ? data[j] <= k : data[j] >= k)) j--;
    	data[i] = data[j];
    
    	while(i < j && (order ? data[i] >= k : data[i] <= k)) i++;
    	data[j] = data[i];
    }
    
    data[i] = k;
    
    quick_sort(data, i, order);
    quick_sort(data + i + 1, n - i - 1, order);
}
```

快速排序算法平均时间复杂度为O(n long n)，空间复杂度为O(log n)，不稳定。









## 结构体多级排序问题





## 通用排序函数：能实现对任意类型、任意长度的数组/结构体进行任意排序。



```C
// 通用排序函数（采用快速排序算法）
void qsort(void* data, size_t n, size_t size, int(*cmp)(const void*, const void*))
{
 // 一趟快速排序处理
 int i = 0, j = n - 1;
    void* k = malloc(size);
    memcpy(k, data, size);

 if(n < 2) return;

 while(i < j)
 {
        while(i < j && (cmp((char*)data + j * size, k) >= 0)) j--;

        memcpy((char*)data + i * size, (char*)data + j * size, size);

        while(i < j && (cmp((char*)data + i * size, k) <= 0)) i++;

        memcpy((char*)data + j * size, (char*)data + i * size, size);
 }

    memcpy((char*)data + i * size, k, size);

    free(k);

 // 分成左右两部分（看成两个不同的数组），分别进行递归处理
 qsort(data, i, size, cmp);
 qsort(data + (i + 1) * size, n - i - 1, size, cmp);
}
```

积累常用标准 C 库函数：

- qsort：通用排序函数















## 查找算法







### 遍历查找



```C
// 在数组 data 的 [start, end) 范围内正向查找 n ，如果找到返回其下标，未找到返回 -1
int search(int* data, int start, int end, int n)
{
    int i;
    
    for(i = start; i < end; i++)
    {
        if(data[i] == n) return i;
    }
    
    return -1;
}
```



遍历查找平均时间复杂度O(n)，空间复杂度O(1)







### 二分查找



目标数据必须是**有序的**，平均时间复杂度O(log n)，空间复杂度O(log n)



```C
// 在数组 data 的 [start, end) 范围内正向查找 n ，如果找到返回其下标，未找到返回 -1
int binary_search(int* data,int start, int end, int n)
{
    int i;
    
    i = (start + end - 1) / 2;
    
    if(start >= end) return -1;
    if(data[i] == n) return i;
    else if(data[i] < n) 
        return binary_find(data, i + 1, end, n);
    else 
        return binary_find(data, start, i, n);
}
```













# 数据结构(Data Structure)



数据结构就是多个元素之间的关系。





四种数据结构：

- 集合
- 线性结构：一对一的关系
- 树：一对多的关系
- 图(网)：多对多的关系





## 线性结构



一对一的关系，一个**有序序列**，也称为线性表。除去第一个数据元素之外，每一个数据元素都有一个唯一的前驱；除去最后一个数据元素之外，每一个数据元素都有一个唯一的后继。



常见线性结构：链表、顺序表、栈、队列、数组、字符串等





### 线性表的两种存储结构：

- 顺序存储结构：按照逻辑顺序连续相邻存储，逻辑上相邻的数据元素在内存中的存储位置也相邻。
- 链式存储结构：数据元素可以存储在任意位置，不要求连续相邻，通过指针体现逻辑上的线性关系。



采用顺序存储结构的线性表称为顺序表，采用链式存储的线性表称为链表。







### 链表(Linked List)



链表是由若干个节点(**Node**)构成的，每个节点由**数据域**和**指针域**两部分构成。数据域用于存放数据元素(比如一个整数、一个学生的信息、一件商品的信息等)，指针域用于存放前驱或后继节点的指针。



尾节点(Tail Node)

头节点(Head Node)



在 C 语言中可以使用 “结构体 + 指针” 实现链表，一个节点用一个结构体表示，数据域用一个或多个数据成员表示，指针域用指针类型的数据成员表示，用它指向当前节点的后继节点或前驱节点。





链表分为三种：

- 单向链表(单链表)：每个节点只有一个后继指针
- 双向链表：每个节点都有两个指针，前驱指针和后继指针，支持双向遍历
- 循环链表：尾节点的后继指针指向头节点，或头节点的前驱指针指向尾节点，逻辑上构成一个环(圆圈)，细分为单项循环链表和双向循环链表



链表支持的操作：

- 创建或初始化(Create)
- 销毁(Destroy)
- 插入
- 删除
- 修改
- 查找
- 清空
- 判空
- 求长度(存放有数据元素节点的数量)
- 排序
- 逆序



在实现链表时，有两种做法：

- 无头链表：最前面的节点也用来存放数据元素，没有专门的头节点
- 有头链表：最前面的节点不用来存放数据元素，专门作为头节点使用

对于无头链表，链表为空表示没有一个节点，链表的长度就是节点数，清空列表就是释放它的所有节点。

对于有头链表，链表为空表示只有一个头节点，链表长度就是节点的总数减一(不算头节点)，清空链表就是释放除头节点之外的其它所有节点。



#### 单向有头链表

```C
typedef int elem_t;    // 声明元素类型

// 声明节点类型
typedef struct node
{
	elem_t data;        // 数据域
    struct node* next;  // 指针域 (后继指针)
    
} node;

// 声明单向链表信息结构体
typedef struct forward_list
{
    node* head;  // 头节点指针，链表最重要的属性
    node* tail;  // 尾节点指针
    size_t len;  // 链表长度(节点数)
    
} forward_list;


// 创建一个新链表(有头链表)
forward_list* create(void)
{
    forward_list* l = (forward_list*)malloc(sizeof(forward_list));
    
    // 有头链表初始是由一个头节点的
    l->head = l->tail = (node*)malloc(sizeof(node));
    l->tail->next = NULL;
    l->len = 0;
    
    return l;
}

// 销毁某个链表
void destroy(forward_list* l)
{
    // 先释放链表的所有节点
    node* p = NULL;
    
    while(l->head != NULL)
    {
        p = l->head->next;
    	free(l->head);
    	l->head = p;
    }
    
    // 释放链表信息结构体
    free(l);
}

// 求长度
size_t size(forward_list* l)
{
    return l->len;
}


// 清空，数据复杂度为 O(n)
void clear(forward_list* l)
{
    // 释放除头节点之外的所有节点
    node* p = l->head->next, *q = NULL;
    
    while(p != NULL)
    {
        q = p->next;
        free(p);
        p = q;
    }
    
    l->tail = l->head;
    l->head->next = NULL;
    l->len = 0;
}


// 判空，时间复杂度为 O(1)
int empty(forward_list* l)
{
    return !(l->len);
}


// 尾插，数据复杂度为 O(1),空间复杂度 O(1)
void push_back(forward_list* l, elem_t data)
{
    // 创建一个新节点
    node* n = (node*)malloc(sizeof(node));
    if(n == NULL)
    {
        perror("malloc fail");
        return;
    }
    n->data = data;
    n->next = NULL;
    
    // 将新节点插入到链表末尾
    l->tail->next = n;
    
    // 更新链表属性
    l->tail = n;
    l->len++;
}


// 头插,时间复杂度为 O(1),空间复杂度为 O(1)
void push_front(forward_list* l, elem_t data)
{
    // 创建一个新节点
    node* n = (node*)malloc(sizeof(node));
    if(n == NULL)
    {
        perror("malloc fail");
        return;
    }
    n->data = data;
    
    // 将新节点插入到链表头部(头节点之后，原来第一个节点之前)
	n->next = l->head->next;
    l->head->next = n;
    
    // 更新链表属性
    if(n->next == NULL) l->tail = n;
    l->len++;
}


// 中插,时间复杂度为 O(1)，空间复杂度 O(1)
void inster(forward_list* l, node* pos, elem_t data) // 在 pos 节点之后插入
{
    node* n = (node*)malloc(sizeof(node));
    
    if(n == NULL)
    {
     	perror("malloc fail");
        return;
    }
    
    n->data = data;
    n->next = pos->next;
    pos->next = n;
    
    if(n->next == NULL) l->tail = n;
    l->len++;
}


// 打印链表所有数据元素
void show_all(forward_list* l)
{
    node* p = l->head->next;
    while(p != NULL)
    {
        printf("%d ", p->data);
        p = p->next;
    }
}



// 遍历链表
void traverse(forward_list* l, int(*visit)(elem_t*)) // visit 为访问函数指针
{
    node* p = l->head-next;
    
    while(p != NULL)
    {
        if(!visit(&(p->data))) break; // 如果访问函数的返回值为0，停止 
        p = p->next;
    }
}
int show(elem_t* data)  // 打印数据元素
{
    printf("%d ", *data);
    return 1;
}
int increment(elem_t* data) // 自增数据元素
{
    ++*data;
    return 1;
}
int save(elem_t* data)    // 写入文件
{
    FILE* fp = fopen("./data", "a");
    
    fprintf(fp, "%d\n", *data);
    
    fclose(fp);
    
    return 1;
}




// 删除,删除 pos 指向节点的后继节点,时间复杂度为 O(1),空间复杂度为 O(1)
void erase(forward_list* l, node* pos)
{	
    node* n = pos->next;
    pos->next = n->next;
    free(n);
    
    if(pos->next == NULL) l->tail = pos;
    l->len--;
}
// 根据数据元素删除节点,时间复杂度O(n),空间复杂度O(1)
int remove(forward_list* l, elem_t data)
{
    node* p = l->head;
    while(p->next != NULL)
    {
        if(p->next->data == data)
        {
            erase(l, p);
            return 1;
        }
        p = p->next;
    }
    return 0;
}



// 查找，返回数据元素指针，否则返回 NULL
elem_t* find(forward_list* l, elem_t data)
{
    node* p = l->head->next;
    while(p != NULL)
    {
        if(p->data == data) 
            return &(p->data);
        p = p->next;
    }
    return NULL;
}



// 逆序
void reverse(forward_list* l)
{
    node* p = NULL, *q = l->head->next, *r;
    l->tail = q;
    while(q != NULL)
    {
        r = q->next;
        q->next = p;
        p = q;
        q = r;
    }
    if(l->head->next != NULL) l->tail = l->head->next;
    l->head->next = p;
}
```







#### 排序



链表排序的最佳算法是归并排序算法。



```C
// 选择排序,单向链表
void sort(forward_list* l, int order) // 0 升序，1 降序
{
    node* p, *q, *k;
    elem_t tmp;
   
    for(p = l->head->next; p->next != NULL; p = p->next)
    {	
        k = p;
        for(q = p->next; q != NULL;q = q->next)
        {
            if(order ? q->data > k->data : q->data < k->data)
                k = q;
        }
        if(k != p)
        {
            tmp = k->data;
            k-data = p->data;
            p-data = tmp;
        }
    }
}
```







#### 双向无头链表

```C

typedef int elem_t;

typedef struct node
{
    elem_t data;
    struct node* next;
    struct node* prev;
} node;

typedef struct list
{
    node* head;
    node* tail;
    int len;
} list;

list* create(void)
{
    list* l = (list*)molloc(sizeod(list));
    l->head = l->tail = NULL;
    l->len = 0;
}

void clear(forward_list* l)
{
    node* p = l->head, *q = NULL;
    
    while(p != NULL)
    {
        q = p->next;
        free(p);
        p = q;
    }
    
    l->tail = l->head;
    l->head = NULL;
    l->len = 0;
}

void destroy(list* l)
{
    clear(l);

    free(l);
}

void push_back(list* l, elem_t data)
{
    node* n = (node*)malloc(sizeof(node));
    if(n == NULL)
    {
        perror("malloc fail");
        return;
    }
    n->data = data;
    n->next = NULL;
    n->prev = l->tail;
    
    if(l->tail == NULL)
    	l->head = n;
    else l->tail->next = n;
 
    l->tail = n;
    l->len++;
}

void push_front(list* l, elem_t data)
{
    node* n = (node*)malloc(sizeod(node));
    
    n->data = data;
    n->next = l->head;
    n->prev = NULL;
    
    if(l->head == NULL)
    {
        l->tail = n;
    }
    else 
    {
        l->head->prev = n;
    }
    l->head = n;
    l->len++;
}

void inster(list* l, node* pos, elem_t data)
{
    if(pos == NULL) 
    {
        push_front(l, data);
        return;
    }
    node* n = (node*)malloc(sizeof(node));
    
    n->data = data;
    n->prev = pos;
    n->next = pos->next;
    
    if(pos->next != NULL)pos->next->prve = n;
    pos->next = n;
    
    if(n->next == NULL) l->tail = n;
    l->len++:
}

// 删除 pos 指向的节点
void erase(list* l, node* pos)
{
    if(pos->prev != NULL) pos->prev->next = pos->next;
    else l->head = pos->next;
    if(pos->next != NULL) pos->next->prev = pos->prev;
    else l->tail = pos->prev;
    
    free(pos);
    l->len--;
}
// 删除元素
void remove(list* l, elem_t data)
{
    node* p = l->head;
    
    while(p != NULL)
    {
        if(p->data = data)
        {
            erase(l, p);
            return 0;
        }
        p = p->next;
    }
}


elem_t* find(list* l, elem_t data)
{
    node* p = l->head;
    while(p != NULL)
    {
        if(p->data == data) 
            return &(p->data);
        p = p->next;
    }
    return NULL;
}

void reverse(list* l)
{
    
}

void sort(list* l)
{
    
}

void traverse()
{
    
}
```





#### 双向链表和单行链表的区别：

- 单向链表的每一个节点只有一个指向后继的指针，所有只支持正向遍历；而双向链表的每个节点有两个指针，前驱和后继，所有支持正向和反向遍历
- 双向链表可以删除指定节点，时间复杂度位O(1)，而单向链表直接删除指定节点的效率很低，时间复杂度O(n)
- 双向链表占用的内存空间更多，实现时需要多维护一个前驱指针，更繁琐









### 顺序表



数组就是顺序表。



顺序表是由若干个同类型的数据元素构成的，所有数据元素按逻辑上的顺序依次连续相邻存储，下标小地址小，下标大地址大，只要首元素指针确定了，其它元素指针可以通过指针加法运算直接获取。





实现顺序表的操作

```C
// 声明数据元素类型
typedef int elem_t;

// 声明顺序表信息结构体
typedef struct vector
{
    elem_t* data;   // 首元素指针
    size_t len;     // 长度(当前元素个数)
    size_t cap;     // 容量(最大元素个数)
} vector;

// 创建
vector* create(size_t cap)
{
    // 堆区数组
    vector* v = (vector*)malloc(sizeof(vector));
    
    if(v == NULL)
    {
        perror("malloc fail");
        return NULL;
    }
    
    v->data = (elem_t*)malloc(cap * sizeof(elem_t));
        
    if(v->data == NULL)
    {
        free(v);
        perror("malloc fail");
        return NULL;
    }
    
    v->len = 0;
    v->cap = cap;
    
    return v;
}

// 销毁
void destroy(vector* v)
{
    free(v->data);
    free(v);
}

// 清空
void clear(vector* v)
{
    v->len = 0;
}

// 判空
int empty(vector* v)
{
    return !v->data;
}

// 求长度
size_t size(vector* v)
{
    return v->len;
}

#define CAP_INCREMENT_STEP 100
void expand(vector* v)
{
    if(v->len == l->cap)
    {
        v->cap += CAP_INCREMENT_STEP;
    	v->data = realloc(v->data, v->cap * sizeof(elem_t));
        
    	if(v->data == NULL)
    	{
    		perror("realloc fail");
        	return;
    	}
    }
}

// 尾插,O(1)
int push_back(vector* v, elem_t data)
{
   // 判满
   if(v->len == v->cap) return 0;
    
   v->data[v->len++] = data;
   return 1;
}
// 尾插，空间不足扩容
void push_back(vector* v, elem_t data)
{
    expand(v);
  
    v->data[v->len++] = data;
}

// 中插，平均时间复杂度O(n)
void inster(vector* v, int pos, elem_t data) // 在 pos 处插入
{
    int i;
    
    expand(v);
    if(pos < 0) pos = 0;
    if(pos > v->len) pos = v->len;
    
    for(i = v->len; i > pos; i--)
    {
        v->data[i] = v-a>data[i - 1];
    }
    
    v->data[pos] = data;
    v->len++;
}

// 头插(空间不足扩容),时间复杂度O(n)
void push_front(vector* v, elem_t data)
{
    inster(v, 0, data);
        
}

// 删除(根据位置删除),平均时间复杂度O(n)
void erase(vector* v, int pos)
{
    int i;
    
    if(pos < 0) pos = 0;
    if(pos >= v->len) pos = v->len - 1;
    
    for(i = pos; i < len - 1; i++)
    {
        v->data[i] = v->data[i + 1];
    }
    
    v->len--;
    
    // 根据实际需求可以进行缩容操作
}

// 查找
int find(vector* v, elem_t data)
{
    int i;
    
    for(i = 0; i < v->len; i++)
    {
        if(v->data[i] == data)
            return i;
    }
    return -1;  // 未找到
}
// 删除(根据值),平均复杂度O(2n)
int erase(vector* v, elem_t data)
{
   // 查找 
   int pos = find(v, data);
   if(pos == -1) return 0;
    
   // 删除
   erase(v, pos);
    
   return 1;
}

// 随机访问数据元素,时间复杂度O(1)
elem_t* at(vector* v, int pos)
{
    if(pos < 0 || pos >= v->len) return NULL;
    return v->data + pos;
}

// 逆序
void reverse(vector* v)
{
    int i, tmp;
    for(i = 0; i < v->len / 2; i++)
    {
        tmp = v->data[i];
        v->data[i] = v->data[v->len - i - 1];
        v->data[v->len - i - 1] = tmp;
    }
}

// 排序（采用插入排序算法）
void sort(vector* v, int order)  // order 为 0 表示升序，非零表示降序
{
        int i, j;
        elem_t k;

        for(i = 1; i < v->len; i++)
        {
                k = v->data[i];

                for(j = i - 1; j >= 0 && (order ? v->data[j] < k : v->data[j] > k); j--)
                {
                        v->data[j + 1] = v->data[j];
                }

                v->data[j + 1] = k;
        }
}


// 正向遍历，时间复杂度O(n)
void traverse(vector* v, int(*visit)(elem_t*))
{
    int i;
    
    for(i = 0; i < v->len; i++)
    {
        if(!visit(v->data + 1)) break;
    }
}
```









### 顺序表和链表的区别(高频面试题)



1. 链表和顺序表都是线性结构，但它们的存储结构不同，链表采用链式存储结构，顺序表采用顺序存储结构；
2. 链表的增加和删除数据的时间复杂度为 O(1)，效率很高；而顺序表增加和删除数据的平均时间复杂度为O(n)，效率很低；如果需要频繁进行增删操作，优先选择链表；
3. 顺序表随机访问的时间复杂度为0(1)，效率很高；而链表随机访问的时间复杂度为O(n)，效率很低；如果需要频繁进行随机访问操作，优先选择顺序表；
4. 链表不要求连续的内存空间，比较灵活，可以充分利用内存碎片；
5. 顺序表的所有内存都可以存放数据元素，内存利用率可达 100%；而链表的每个节点都要投入一部分内存空间存放额外的指针。







### 栈(Stack)



栈是一种具有 LIFO 特点的，操作受限的线性表。



LIFO：Last In First Out，后进先出

FILO：First In Last Out，先进后出



相关专业术语：

- 入栈(Push)
- 出栈(Pop)
- 栈顶(Stack Top)
- 栈底(Stack Bottom)



栈支持的操作：

- 创建
- 销毁
- 清空
- 判空
- 求长度
- 入栈：在栈顶插入的数据元素
- 出栈：删除栈顶元素
- 访问栈顶元素





栈的两种实现方式：

- 基于顺序表实现，即顺序栈
- 基于链表实现， 即链栈





```C
// 无头单向链表实现

typedef int elem_t;

typedef struct node
{
    elem_t data;
    struct node* next;
} node;

typedef struct stack
{
    node* head;  // 栈顶指针，即链表头节点指针，最重要的属性
    size_t len;  // 栈长度
    size_t cap;  // 栈容量
} stack;

stack* create(size_t cap)
{
    stack* s = (stack*)malloc(sizeof(stack));
    if(s == NULL)
    {
        perror("malloc fail");
        return NULL;
    }
    
    s->head = NULL；
    s->len = 0;
    s->cap = cap;
    
    return s;
}


void clear(stack* s)
{
    node* p = s->head, *q = NULL;
    
    while(p != NULL)
    {
        q = p->next;
        free(p);
        p = q;
    }
    s->head = NULL;
    s->len = 0;
}

void destroy(stack* s)
{
    clear(s);
    free(s);
}

int empty(stack* s)
{
    return !(s->len);
}
size_t size(stack* s)
{
    return s->len;
}


// 入栈(头插),时间复杂度O(1)，返回 0 成功，非零失败
int push(stack* s, elem_t data)
{
    if(s->len == s->cap) return 1;
    
    node* n = (node*)malloc(sizeof(node));
    if(n == NULL)
    {
        perror("malloc fail");
        return 2;     
    }
    
    n->data = data;
    n->next = s->head;
    
    s->head = n;
    s->len++;
    
    return 0;
}

// 出栈，删除头节点
// 注意：调用本函数必须保证栈非空，否则结果不可预知
int pop(stack* s)
{
    node* p = s->head->next;
    s->head = p->next;
    free(p);
        
    s->len--;
}

// 访问栈顶元素,O(1)
// 注意：调用本函数必须保证栈非空，否则结果不可预知
elem_t* top(stack* s)
{
    return &(s->head->data);
}
```









### 队列(Queue)





一种具有 FILO 特点的，操作受限的线性表



相关专业术语：

- 入队(Push Back)
- 出队(Pop Front)
- 队头(Queue Front)
- 队尾(Queue Back)



队列支持的操作：

- 创建
- 销毁
- 清空
- 判空
- 求长度
- 入队：在队尾插入新数据元素
- 出队：删除队头元素
- 访问对头和队尾元素



普通队列只能在队尾入队，队头出队。双端队列(Double End Queue)支持在队列两端入队和出队。



队列的两种实现方式：

- 基于顺序表实现，即顺序队列
- 基于链表实现，即链队列





```C
// 基于顺序表实现队列(循环队列)

typedef int elem_t;

typedef struct queue
{
    elem_t* data;
    int head, tail;  // 队头队尾位置
    size_t len;      // 长度(当前元素个数)
    size_t cap;      // 容量(最大元素个数)
} queue;

queue* create(size_t cap)
{
    queue* q = (queue*)malloc(sizeof(queue))
    if(q == NULL)
    {
        perror("malloc fail");
        return NULL;  
    }
    
    q->data = (elem_t*)malloc(sizeof(elem_t));
    if()
    {
        perror("malloc fail");
        free(q);
        return NULL
    }
    
    q->head = q->tail = 0;
    v->cap = cap;
    q->len = 0;
    
    return v;
}

// 销毁
void destroy(queue* q)
{
    q->head = q->tail = 0;
 	q->len = 0;   
}

// 判空
int empty(queue* q)
{
    return !(q->len);
}

// 判满
int full(queue* q)
{
    return (q->len == q->cap);
}


// 入队(尾插)
// 注意：调用本函数必须保证队列未满，否则结果不可预知！
int push_back(queue* q, elem_t data)
{
    q->data[v->tail] = data;
    q->tail = (q->tail + 1) % q->cap;
    q->len++;
    
    return 1;
}

// 出队
// 注意：调用本函数必须保证队列非空，否则结果不可预知！
int pop_front(queue* q)
{
    q->head = (q->head + 1) % q->cap;
    q->len--;
}

// 访问队头元素
elem_t* front(queue* q)
{
    return q->data + q->head;
}
// 访问队尾元素
elem_t* back(queue* q)
{
    return q->data + (q->tail - 1 + q->cap) % q->cap;
}

// 随机访问数据元素
elem_t* at(queue* q, int pos)
{
    if(pos < 0 || pos >= q->len) return NULL;
    
    return q->data + (q->head + pos) % q->cap;
}

// 遍历
void traverse(queue* q, int(*visit)(elem_t*))
{
    int i;
    for(i = 0; i < q->len; i++)
    {
        if(!visit(q->data + (q->head + i) % q->cap)) break;
    }
}
```













## 树(Tree)





一对多的关系。

树是一种分支层次关系，如人类族谱、公司组织架构、文件系统等。





树相关的概念：

- 根节点(Root Node)
- 父节点和子节点
- 叶子节点(终端节点)：度为 0 的节点
- 分支节点(非终端节点)：度大于 0 的节点
- 双亲节点(Parent Node)和孩子节点(Child Node)
- 树的深度(高度)：树的最大层数
- 节点的度：节点的分支数量
- 树的度：树中所有节点的最大度数



树可以为空，即空树。

树是由根节点和若干棵子树构成。





任意一棵树都可以通过某种规则转换为一颗二叉树。





### 二叉树(Binary Tree)



二叉树是一种度不超过 2 的有序树。



二叉树是由根节点、左子树、右子树构成的，左子树和右子树也是二叉树。



二叉树的五种形态：

- 空二叉树
- 只有一个根节点
- 根节点和左子树
- 根节点和右子树
- 根节点和左子树和右子树



各种不同的二叉树：

- 满二叉树：各层节点数量都达到最大值
- 完全二叉树：除了最后一层，其它节点数量都达到最大值，并且最后一层的所有节点必须靠左连续排列。
- 二叉搜索树(BST)：左孩子 <= 根节点 <= 右孩子
- 平衡二叉树：左右子树的高度差不超过 1
- 平衡二叉搜索树
- 红黑树：平衡二叉搜索树的一种实现
- 堆(Heap)：分为大根堆(大顶堆/最大堆)和小根堆(小顶堆/最小堆)，堆一定是一棵完全二叉树，完全二叉树不一定是堆

满二叉树绝对是完全二叉树，但完全二叉树不一定是满二叉树

> 数据结构中的堆和内存中的堆区是不同的概念。



完全二叉树天生适合顺序表实现：整棵树的根节点的下标为 1 ，假设某个节点的下标为 i ，那么它的左孩子的下标一定为 2\*i ，右孩子的下标一定为 2\*i+1，父节点下标一定为 i/2





二叉树的一些特性：

- 第 n 层最多有 2ⁿ⁻¹ 个节点
- 高度为 n 的二叉树最多有 2ⁿ - 1 个节点
- 如果度为 0 的节点数量为 n0 ，度为 2 的节点数量为 n2，那么n0=n2+1 成立











#### 二叉树的 C 语言实现



```C

typedef char elem_t;

// 声明节点类型
typedef struct node
{
    elem_t data;
    struct node* parent;  // 父节点指针，可选
    struct node* left, *right; // 左孩子指针、右孩子指针
} node;


// 创建特定二叉树
node* create(void)
{
    char c;

    c = getchar();
    if(c == ' ') return NULL;

    // 创建根节点
    node* root = (node*)malloc(sizeof(node));
    root->data = c;
    // 创建左子树
    root->left = create();
    // 创建右子树
    root->right = create();

    return root;
}
// 销毁特定二叉树
void destroy(node* bt)
{
    if(bt == NULL) return;
    // 销毁左子树
    destroy(bt->left);
    // 销毁右子树
    destroy(bt->right);
    // 释放根节点
    free(bt);
}
```







#### 二叉树的遍历



- 先序遍历(先根遍历)：根左右
- 中序遍历(中根遍历)：左根右
- 后序遍历(后根遍历)：左右根
- 层序遍历：从上往下，从左到右依次访问每个节点

通过中序遍历和先序遍历/后序/层序遍历序列可以确定唯一二叉树结构



深度优先遍历(先序/中序/后序)和广度优先遍历(层序)











## 图



多对多的关系(m : n)



最短路径优先算法。