# 面试题









## static 的作用





作用域和生存期



- 非静态局部变量和静态局部变量的作用域一样(都是代码块局部范围)，但是生存期不一样，非静态局部变量的生存期是从定义时开始到所在代码块执行结束（“短命鬼”），静态局部变量的生存期是程序的整个执行期间（“长生不老”）。  
- 非静态全局变量和静态全局变量的生存期一样，都是程序的整个执行期间(“长生不老”），但是作用域不一样，静态全局变量的作用域为它所在的文件(文件范围)，而非静态全局变量的作用域为整个项目范围(项目中的所有文件)
- 函数的作用域和生存期与全局变量相同，生存期都是整个程序的整形期间。根据作用域不同，分为静态函数(static修饰)和非静态函数。静态函数的作用域是它所在的原文件，非静态函数的作用域是整个项目。在不同文件中，静态函数可以同名，而非静态函数在整个项目中都不能同名(重复定义错误)。



如何选择？

- 能用局部变量，不用全局变量！尽量少用或不用全局变量(非必要不用全局变量)，因为全局变量会增加功能模块之间的耦合度，作用域很大，很容易造成同名冲突。需要用全局变量的情况：多个函数需要共享访问一个变量时就只能用全局变量，优先选择静态全局变量，除非这些函数在不同的文件中。
- 能用非静态局部变量，不用静态局部变量！
- 能用静态函数就不用非静态函数，一个函数如果只需要在模块内部调用(一个文件内部)，就应该使用静态函数，所以静态函数也称为模块私有函数。如果一个函数确实需要被不同模块调用(跨文件调用)，就只能使用非静态函数(要尽量少)。







## 指针常量和常量指针的区别



地址无法改变

值无法改变

指针常量：指针类型的常量，指针本身是常量，但它指向的数据不是常量。
常量指针：常量的指针，指针本身是变量，但它指向的数据是常量(在它心中)。
常量指针常量：常量指针类型的常量，指针本身是常量，它指向的数据也是常量(在它心中)





## 实现 strcpy 函数



形参只读，用 const 修饰



```c
char* strcpy(char* dst, const char* src)
{
    char* ret = dst;
    while(*dst++ = *src++);
    return ret;
}
```



```C
// strcpy 返回值的意义：构成表达式，参与下一步运算处理，功能更强，如果返回值为 void 就不能用了
char buff1[100];

strcpy(buff1, "dongge");

strcpy(strcpy(buff1, "mage"), "junge");

char* p = strcpy(buff1, "china") + 2;

printf("%s\n", p);  // ina

```













## 链表和顺序表的区别



1. 链表和顺序表都是线性结构，但它们的存储结构不同，链表采用链式存储结构，顺序表采用顺序存储结构；
2. 链表的增加和删除数据的时间复杂度为 O(1)，效率很高；而顺序表增加和删除数据的平均时间复杂度为O(n)，效率很低；如果需要频繁进行增删操作，优先选择链表；
3. 顺序表随机访问的时间复杂度为0(1)，效率很高；而链表随机访问的时间复杂度为O(n)，效率很低；如果需要频繁进行随机访问操作，优先选择顺序表；
4. 链表不要求连续的内存空间，比较灵活，可以充分利用内存碎片；
5. 顺序表的所有内存都可以存放数据元素，内存利用率可达 100%；而链表的每个节点都要投入一部分内存空间存放额外的指针





## 求是否是素数



```C

int is_printf(int num)
{
    int i;
    if(num < 2 || (num > 2 && num % 2 == 0)) return 0;
    
    for(i = 3; i * i <= num; i += 2)
    {
        if(num % i == 0) return 1;
    }
    
    return 0;
}
```











## 堆是什么





堆区(Heap)：容量很大，由我们程序亲自管理分配和释放，很灵活，非常适合在内存中处理大量数据



堆(Heap)：分为大根堆(大顶堆/最大堆)和小根堆(小顶堆/最小堆)，堆一定是一棵完全二叉树，完全二叉树不一定是堆









## 判断链表中是否存在环



快慢指针

```C
int is_loop(struct node* lead)
{
    struch node* fast = head, *slow = head;
    
    while(fast != NULL && fast->next != NULL)
    {   
        slow = slow->next;
        fast = fast->next->next;
        
        if(fast == slow) return 1;
    }
    
    return 0;
    
}
```

两指针相遇时，slow 回到头节点，之后 fast 和 slow 同时移动相同步长，再相遇时为环的入口









## 栈是什么





栈区(Stack)：容量很小(若干兆空间，很容易栈溢出)，系统自动管理分配和释放，系统不会进行初始化，用于存放形参变量、非静态局部变量等



一种操作受限的线性表，先进后出









## 运算符优先级问题

```C
int main()
{
    int i;
    unsigned char a = 4;
    
    for(i = 0; i < 4; i++)
    {
        if(a & 0xf0 != 0) break;
        else a <<= 1;
    }
    
    printf("%d\n", a);
    
    return 0;
}
```

### 运算符优先级：

- 在写表达式时，优先级拿不准，千万别乱猜，多写括号累不死！
- 单目运算符优先级高于双目运算符，记住：独眼龙比两只眼的正常人更狠！
- 三目运算符( ?  : )：优先级倒数第三！
- 赋值运算符的优先级倒数第二！
- 逗号运算符的优先级最低！
- 对应数码运算符，算数运算符 > 比较运算符> 位运算符 > 逻辑运算符 ，记住四个字：**算比位逻**
- 记住三个字：非与或！









## 宏和函数的区别





普通宏，带参宏(和函数用法相同)，预处理阶段处理掉，没有额外的时间开销



函数调用在编译时处理，会分配栈区，返回值，有额外的时间空间开销











## X & (X - 1)问题

```C
int func(int x)
{
    int countx = 0;
    while(x)
    {
        countx ++;
        x = x & (x-1);
    }
    return countx;
}

x &\^ (x - 1) 问题
x &\^ -x

```











## 队列和栈的区别





都能用链表和顺序表实现



栈先进后出，队列先进先出









## 输入一段任意英文内容，统计其中出现次数最多的字符



```C
int main()
{
	char text[1001];

	scanf("%1000[^\n]", text);
	
	int char_cnt[128] = {0};  // 统计每个英文字符的出现次数，初始都是 0
	char max = '\0';
	int i;
	
	for(i = 0; text[i] != '\0'; i++)
	{
		char_cnt[(int)text[i]]++;
	}

	for(i = 0; i < 128; i++)
	{
		if(char_cnt[i] > char_cnt[(int)max])
			max = i;
	}

	printf("出现次数最多的字符为：%c\n", max);

	return 0;
}
```









## sizeof 和 strlen 的区别





sizeof 是编译时运算符，编译时求值，计算数据所占内存空间总大小，对字符串运算时包括'\0'



strlen 是标准 C 库函数，运行时求值，计算字符串不包括'\0' 









## 内存泄漏



调用的内存占用的的内存没有释放





## 内存溢出



Memory Overflow

访问了超出目标内存空间范围之外的内存位置，会造成不可预知的结果！



技术网站:https://stackoverflow.com













## 转义字符



'\x41' :x后一到两位 16 进制数字

'\101' ：一到三位 8 进制数字

'\n' '\0' '\r'

strlen: "junge\x41 123yyds\101!"







## 野指针和悬空指针



野指针：未初始化的指针，指向不确定的内存位置，对野指针解引用访问会造成不可预知的结果！



悬空指针：指向已经释放的内存空间的指针，解引用访问会造成不可预知的结果！









## malloc 和 calloc 的区别

- malloc 只有一个参数，表示需要申请分配的堆区空间的字节数；而 calloc 有两个参数，第一个参数为需要申请分配的数据元素的个数，第二个为每个数据元素的宽度。calloc 更适合用于堆区数组。
- malloc 不会进行初始化，而 calloc 会进行清零初始化。
- malloc 效率更高