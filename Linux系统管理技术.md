# linux管理技术



---

## linux系统简介



### 操作系统：Operating System，OS，**管理软硬件资源**的系统软件，通常使用C语言编写实现。

Linux 创始人：linus

linux内核开源免费（遵守GPL协议），任何个人或组织都可以轻松获取，根据自己需求修改发布全新的操作系统新的系统成为Linux发行版，都称为Linux系统，内核都是Linux。

> linux 内核官网： https://www.kernel.org

#### Kernel: 内核，操作系统内部核心功能（内存管理、任务管理、文件系统、网络协议栈等）

### Linux应用领域：
- 服务器（Server）
- 嵌入式设备（Embedded Device）

### 常用Linux发行版：
- 深度（Deepin）
- 统信（UOS）
- 麒麟（Kylin）
- 红旗（RedFlag）
- Ubuntu
- CentOS
- RedHat
- Suse
- Debian
- Kali

## Linux安装和使用

### Linux系统安装方式：
- 真机直接安装
- 双系统或多系统
- 虚拟机安装

在Vmware虚拟机中安装Ubuntu
        需要文件：
- VMware安装包
- Ubuntu系统镜像文件（.iso）
Linux系统使用方法：
- 桌面环境：友好直观，傻瓜式操作，但功能较弱并且很低效，桌面环境不是Linux内核的一部分，很多Linux系统默认没有安装桌面环境（比如服务器和嵌入式Linux系统），桌面环境需要占用很多硬件资源，非必要不使用
- Shell命令：功能强大并且高效，但不够友好直观，需要记忆一堆命令。
常用桌面环境:GNOME、KDE等。
Shell:外壳，用户与内核交互的界面。

## 常用Shell命令

**man**:manual,帮助手册，通过该命令可以查看命令或函数的使用方法。
clear：清屏
reset：重置终端
echo：打印信息，单独使用意义不大，配合其它命令一起使用
wget：从网络上下载文件，常用参数 -O 指定下载后的文件名

几乎所有命令都支持参数，命令名和参数之间以及不同参数之间要用空格（可以多个）分隔，如果某个参数内容中含有空格，那必须用单引号或双引号将该参数括起来。命令的参数支持短风格和长风格形式，功能一样。

### 按功能分类

#### 电源管理：
- reboot(重启)
- poweroff(关机)
- shutdown(关机或重启)  -r 参数表示重启，-h 表示关机

#### 网络配置：
- ping:测试本机与目标机器之间的网络是否通畅，通过该命令获知网络连接是否有、网速如何、网络是否稳定(通过丢包率衡量)，很多网络通信通过调用系统ping命令，获得平均往返时间表示网速。
- ip:设置或查看本机网络配置，常用参数 a
- ifconfig:查看或设置本机网络配置
- netstat:查看本机所有网络连接信息

#### 软件包管理

##### Linux系统中安装软件包的常用方式：
- 在线安装，使用软件包管理器从软件源服务器下载软件包然后进行安装
- 手动下载软件包到 本地进行安装
- 可执行文件(绿色软件，解压直接运行)
- 源码包(自己下载软件源码，解压编译安装，需要安装编译构建环境)

Debian/Ubuntu系列发行版(包括Deepin)使用的软件包管理器是apt，软件包格式.deb；Redhat/CentOS/Fedora系列发行版使用yum，软件包格式.rpm。
>老版本Debian/Ubuntu使用apt-get

为了提升卸载软件包速度，建议进行换源操作。

##### Ubuntu系统上软件包管理常用命令：
- apt update:更新本地软件包列表信息（即软件包名字和最新版本号）
- apt upgrade:将当前系统中已安装的所有软件包升级到最新版本
- apt install:安装某个软件包，如软件包已经安装，就将它升级到最新版本
- apt remove:卸载某个已安装的软件包
- apt list --installed:查看所有已安装的软件包

在Linux系统中有些命令普通用户没有权限执行，只有root用户才能执行，此时可以在该命令的最左面加上sudo前缀，表示以root用户执行该命令。

root用户是Linux系统内置的超级管理员用户，具有最高权限。



### 用户和组群管理

现代操作系统（如Linux、Windows、MAC等）都支持多用户。

用户(User)：使用系统的一个身份标识。
组群(Group)：方便批量管理用户和用户之间的通信。

root用户是Linux系统内置的用户，它是系统管理员用户具有最高权限。除了root用户外，可以自己创建用户。每个用户都有自己独立的配置和工作环境，互不影响。

在Linux系统中，每个用户必须有且只有一个主组群(也称为初始组群)，可以加入任意个其他组群。


>管理就四个字：增删改查(CRUD)

#### 组群管理命令：
- groupadd:group add,创建一个新的组群
- groupdel:group delete,删除一个组群
- groupmod:group modify,修改一个组群的属性信息(如GID、组群名等)
- cat /etc/group:查看系统在所有组群的信息，组群信息存放在 /etc/group文件中

#### 用户管理命令：
- useradd:user add，创建一个新用户
- userdel:user delete，删除用户，常用参数:-r表示将用户的主文件夹以及邮件文件一并删除
- usermod:user modify，修改用户属性信息(UID、用户名、主目录、登录Shell等)
- cat  /etc/passwd:查看系统在所有用户的信息，用户信息存放在 /etc/passwd文件中
- id: 查看用户主组群，组群，id等
- passwd:给用户设置密码，Linux系统不允许空密码用户登录系统，用户密码加密后放在/etc/shadow(影子文件)，该文件默认只有root用户可读
- su:select user，切换当前用户,不指定用户则切换到root用户
- exit:注销当前用户,常用参数：- 表示同时切换当前文件夹为目标用户的主文件夹并且应用目标用户的环境变量

创建用户时如果没有指定主组群，Linux系统会自动创建一个与用户同名的组群 作为主组群
每个用户通常都有一个自己的主文件夹（主目录），用户主文件夹的名字通常和用户名相同，普通用户的主文件夹通常在 /home下，root用户主文件夹是 /root
>属性信息也称为字段
> 文件夹也叫目录
常用Shell：
- bash
- sh
- csh
- ksh
- nologin (无登录shell)
- ...


### 文件系统管理

文件系统:File System，管理磁盘上的文件数据的程序。

#### Linux系统中文件分为7种类型：
- 普通文件
- 文件夹
- 软链接
- 管道文件
- 套接字文件
- 设备文件
- 块设备文件

#### **==Linux设计哲学：一切皆文件==**

根文件夹(Root Directory):也称根目录,Linux系统最顶层的文件夹,用 / 表示。 
文件路径(File Path):描述一个文件在磁盘上的存储位置。只要知道一个文件的路径，就能找到它和操作它。

文件路径有两种表示形式：
- 绝对路径：基于根文件夹定位，如：/home、/bin/bash、/root/music
- 相对路径：基于当前文件夹定位，用 . 表示当前文件夹，用 .. 表示当前文件夹的上一层文件夹。如：./a.out、../music/a.mp3、../../../jg

>在Linux系统中根文件夹以及路径分隔符都用“/”表示

每个文件夹下面都有两个特殊的隐藏文件夹，名字是.和..，前者就是当前文件夹，后者就是当前文件夹的上一级文件夹

#### 文件夹操作常用命令：
- pwd： print working directory，打印当前文件夹
- cd：change directory，改变当前文件夹
- mkdir：make directory，创建一个新的文件夹，-p参数创建多级文件夹
- rmdir：remove directory，删除空文件夹，用得很少，更多使用rm -rf可以删除任意文件夹

#### 普通文件操作命令：
- touch：将目标文件的最近修改时间更新为当前时间，如果目标文件不存在，它就会创建一个新的文件（0字节）
- 使用重定向符可以创建新文件，如：`echo "12345" > data`
- 使用文本编辑器（vi、vim、nano等）
- cat/more/less：查看文件数据，cat适合查看小文件，more 和 less 适合查看大文件，支持浏览控制，less 比 more更强更好用
- tail：查看文件结尾的若干行内容，默认显示10行，通过-n指定显示内容
- head：查看文件开头若干行内容，默认显示10行，通过-n指定显示内容
- grep：在文件中查找某个数据，常用参数：-n表示行号

#### 文件夹和普通文件通用命令：
- rm：remove，删除文件夹时通常使用-rf参数
- ls：list，查看某个文件夹下(或文件)的文件信息，-l参数显示文件的详细属性信息，-a(all)参数显示所有文件包括隐藏文件(文件名以 . 开头的文件)，-d参数表示查看文件夹自己的属性信息
- chmod：change mode，修改文件模式信息(即文件操作权限信息)，有两种用法形式：1.使用三个八进制数字表示三组权限；r用4表示， w用2表示，x用1表示  2.使用u(所有者)、g(同组者)、o(其他人)、a(所有人)、r、w、x、+、-、=表示三组权限
- chown：change ownership，修改文件所有者和所属组群
- cp：copy，拷贝文件，拷贝文件夹是必须带上 -r 参数
- mv：move，移动文件，通常用该命令实现文件重命名操作(在同一个文件夹下移动就可以实现重命名)

#### 详解ls -l命令的执行结果（文件的详细属性）：
- 第1列第1个字符表示文件类型，有7种类型，-表示普通文件，d表示文件夹，l表示软链接
- 第1列后面9个字符表示文件操作权限，3个字符为一组，共表示3组权限，第1组表示所有者，第2组表示同组人的操作权限，第3组表示其他人的操作权限。每一组的权限依次为r(读)、w(写)、x(执行)，如果没有某个权限用其对应位置用 - 表示 
- 第2列表示硬链接数。💡补充：对普通文件表示该文件的硬链接个数；对文件夹表示其一级子文件夹的个数（含 . 和 ..）
- 第3列表示文件的所有者(所属用户)，默认为创建该文件的用户
- 第4列表示文件的所属组群，默认为所有者的主组群
- 第5列表示文件大小，默认以字节为单位，带上-h参数可以以更可读的形式显示
- 第6列表示文件的最近修改时间
- 最后一列表示文件名

#### 文件的r、w、x权限的具体意义：
- 对于普通文件，r权限表示可以读取其数据内容，如cat、more、less命令；w权限表示可以修改其数据内容，如vim、nano命令；x权限表示可以执行该文件，如果该文件不是可执行文件，即使具有x权限也没有实际意义。
- 对于文件夹，r权限表示可以读取其数据内容，如ls命令；w权限表示可以在它内部创建或删除文件，如touch、rm命令；x权限表示可以打开该文件夹，如cd命令。==x 权限是r 和 w 权限的基础。==

==在Linux系统中，每个文件都有一个所属用户(主人)和所属组群==

重定向符（>和 >>）：将其左边命令的执行结果写入到文件中，而不是在终端窗口显示。如果目标文件不存在就自动创建一个新文件，如果目标文件已存在，>会清空文件中原有的数据再写入新数据，>>会保留文件数据并写入新数据

解读终端(/bin/bash)提示符的意义：当前用户名@主机名:当前文件夹\$/#,\$表示普通用户，\#表示管理员用户

#### Linux系统根目录下各文件的作用：
- /etc：存放各种系统配置文件
- /home：存放普通用户的主目录
- /root：root用户的主目录
- /bin：binary，存放所有用户能执行的命令(程序)
- /sbin：super binary，存放系统管理员能执行的命令(程序)
- /boot：存放 Linux 系统启动所需要的重要文件
- /dev：device，存放各种设备文件
- /lib：存放各种库文件


### 软件UI分为两种：
- GUI：Graphical User Interface，图形化用户界面
- CUI：Character User Interface，字符化用户界面


vim是vi的增强版，功能和用户体验比vi更强，现在基本使用vim。vi/vim的核心用法：两个常用模式，命令模式和插入模式，命令模式用于执行各种命令（如保存、退出、粘贴、复制、查找、替换等），插入模式用于输入数据内容。初始处于命令模式下，按下 i 键可以切换到插入模式下，编辑数据内容完毕后按下ESC键可以切换到命令模式下，然后输入:wq就可以保存并退出，输入:q!就可以不保存退出。


### 链接文件(Link File):
- 软链接(Symbolic Link):也称为符号链接，其数据内容就是另一个文件的路径，我们称它指向另一个文件，用 l 表示其类型，和Windows上的快捷方式一样。使用 ln -s 命令创建，建议使用绝对路径。
- 硬链接(Hard Link):硬链接就是一个名字，使用 ln 命令创建。不能给文件夹创建硬链接。

> 文件就是存放在磁盘上的一份数据，文件名就是这份数据的一个标签，用于识别这份数据，方便访问。一个文件是可以有多个文件名的，这些文件名都称为该文件的硬链接。

### 文件打包和压缩

文件打包：为了方便管理和传输，将多个文件整合在一起构成一个文件，这个文件就是包文件，后缀名通常为 .tar 
文件压缩：使用某个数据算法将一个文件变小，减少存储空间占用，提升网络传输效率，压缩文件不能直接使用，需要进行解压缩
压缩包：将多个文件打包在一起得到的包文件，然后对包文件进行压缩处理得到的最终压缩文件
常用压缩算法：zip、gzip、bzip、xz、7z、~~rar~~(闭源)

文件压缩和解压常用命令：
```shell
# gzip 压缩和解压  
gzip junge.jpg # 得到压缩后的文件 junge.jpg.gz  
gunzip junge.jpg.gz # 得到原始文件 junge.jpg

# bzip2 压缩和解压  
bzip2 junge.jpg # 得到压缩后的文件 junge.jpg.bz2  
bunzip2 junge.jpg.bz2 # 得到原始文件 junge.jpg

# xz 压缩和解压  
xz junge.jpg # 得到压缩后的文件 junge.jpg.xz  
unxz junge.jpg.xz # 得到原始文件 junge.jpg
```

#### 常用压缩包格式及操作命令:
```shell
#.tar.gz 或.tgz格式
tar -zcvf junge.tar.gz hello.c .....   #打包并压缩
tar -tvf junge.tar.gz                  #查看压缩包
tar -xvf junge.tar.gz                  #解压缩，默认为当前文件夹，-C指定文件夹

#.tar.bz2 格式
tar -jcvf junge.tar.bz2 hello.c .....   #打包并压缩
tar -tvf junge.tar.bz2                  #查看压缩包
tar -xvf junge.tar.bz2                  #解压缩，默认为当前文件夹，-C指定文件夹

#.tar.xz 格式
tar -Jcvf junge.tar.xz hello.c .....   #打包并压缩
tar -tvf junge.tar.xz                  #查看压缩包
tar -xvf junge.tar.xz                  #解压缩，默认为当前文件夹，-C指定文件夹

#.zip 格式
zip -r junge.zip hello.c ....          #创建压缩包
unzip -l junge.zip                     #查看压缩包
unzip junge.zip                        #解压缩，默认解压到当前文件夹下，可以通过 -d 参数指定解压路径
```

#### 文件打包命令：
```shell
#打包
tar -cvf junge.tar hello.c mage.jpg 32321.rter ....
#查包
tar -tvf
#解包
tar -xvf junge.tar -C(指定解包目录)
```


### 文件名通配符：
- *：表示若干个任意字符(可以为0个)
- ?：表示一个任意字符
> 文件系统管理命令都支持文件名通配符，从而可以轻松实现批量文件操作


### 任务(进程)管理

现代操作系统，Linux、Windows都是多任务操作系统。

一个任务通常就是一个进程(Process)，进程就是正在运行的程序，程序就是可执行文件。
操作系统给每个进程都分配了一个唯一的整数编号，就是PID(Process Identification),用于标识区分不同的进程

>软件(Software)：程序 + 文档
> 硬件(Hardware)：PCB板、芯片、元器件等

命令就是程序，通常放在 /bin 和 /sbin 下，通过which 命令查看命令路径

#### 进程管理命令：
- ps：process snapshot，进程快照，查看系统在当前时刻所有进程的信息，不带任何参数查看的是在当前终端中运行的进程信息，带上-e或-aux参数查看系统中所有进程信息
- top：实时刷新显示系统中所有进程信息
- kill：通过PID结束某个进程，参数 -9
- killall：通过程序名批量结束进程
- fg：forground，将当前正在后台执行的程序调的前台执行
- bg：background，让当前在后台暂停的进程执行起来

CTRL + C组合键：结束当前终端中正在前台运行的进程。
CTRL + Z组合键：将当前正在前台执行的进程调到后台并暂停它
&：在命令末尾加上该符号，可以让命令在后台执行


### 服务(Service)管理

服务就是一些在系统后台运行的程序，如Web服务、FTP服务、邮件服务等。

服务常用命令：
```shell
sudo systemctl start 服务名     #启动服务

sudo systemctl stop 服务名      #停止服务

sudo systemctl restart 服务名   #重启服务

sudo systemctl enable 服务名    #将指定服务设置为开机自启

sudo systemctl disable 服务名   #取消开机自启

sudo systemctl status 服务名    #查看服务状态
```


### 环境变量(Environment Variables)

系统运行环境中的一些变量，它们可能会影响系统或应用程序的行为。

环境变量相关常用命令：
- env：查看所有环境变量
- 使用 echo 命令可以查看某个环境变量的值，注意：引用环境变量时必须要在它名字的左边写上 $ 符号
- export：设置环境变量，如：export name=junge
>💡补充：用 export 设置的变量只在当前终端会话中生效；想要永久生效，需要把它写入配置文件（如 ~/.bashrc），或用 export 命令配合写入后执行 `source ~/.bashrc` 使其立即生效。


URL:Uniform Resource Locator，统一资源定位符，俗称“链接”或“网址”

### 重启网络服务：
```shell
systemctl stop NetworkManager
rm /var/lib/NetworkManager/NetworkManager.state
systemctl start NetworkManager
```




## Shell 命令进阶技巧

| ：管道符，将左边命令的执行结果传给右边的命令处理(通常作为文件处理)，如：ps -e | grep firefox

&& ：先执行左边命令，如果左边命令执行成功就接着执行右边命令，否则不执行右边命令

； ：先执行左边命令，无论是否执行成功都会接着执行右边命令


## Shell脚本

将需要执行的若干条命令写入一个文件保存，后面只需要执行该文件就自动将它里面的所有命令从上往下自动执行一次，这个文件就是 Shell 脚本程序后缀名通常为 .sh

Shell 脚本中可以直接使用 Shell 命令作为语句，支持变量、常量、流程控制、函数等功能，所以它也是一门编程语言。非常适合实现各种系统管理任务。

```shell
#!/bin/bash   #指定解释器
```
>学习要求：学会编写 Shell 脚本，看懂修改 Shell 脚本

编程语言分为两类：
- 解释型语言：也称为脚本语言，源代码可以直接运行，不需要编译，需要解释器，在运行时边翻译边执行，所以效率较低。如Shell、Python、PHP、JavaScript、Lua等。
- 编译型语言：源代码不能直接运行，必须通过编译器处理转换为二进制可执行文件才能执行，所以效率较高。如C 、C++ 、Java 、C#等。
